# 更新日志

> 按时间倒序记录功能变更。

## v1.0.8 (2026-09-08) · 拍照裁剪闪退修复 + 公式失败可诊断

- **第一次拍照有概率闪退裁剪界面（第二次正常）**：待裁剪队列 `cropQueue` 用普通 `remember` 存，部分 ROM（MIUI/鸿蒙）对 `requestedOrientation` 的每次写入都可能触发界面重建，队列一丢刚弹出的裁剪界面就消失；第二次拍照时方向没有变化、没有写入动作，所以正常。修复：队列/打卡照片列表改 `rememberSaveable` 存路径（重建不丢）；方向守护在「当前方向本来就没被相机带偏」时跳过强制转向写入，只交还系统；
- **公式失败占位可诊断**：占位框标题现在带异常类型（如 `⚠ 公式无法渲染 (ParseException)`），失败的完整 latex 与异常一并写入本地 `assistant-render.log`——再遇到占位框，一张截图就能定位失败原因（此前只有 60 字符片段，重建不出真实失败公式）；
- 排查结论（供参考）：`\begin{cases}` 环境、`\operatorname`、段首 `=`、段首 `\\` 等候选形态经真实解析器逐一验证全部可解析（已固化为回归断言），真机失败的 latex 与表面看到的不同，只能靠现场取证定位。

## v1.0.7 (2026-09-07) · 裁剪弹窗横屏修复 + 公式裸环境修复

- 拍完照的裁剪弹窗在（平板）横屏下上下被裁、看不到比例条和保存按钮：根因是视口约束写成 `fillMaxWidth().widthIn(max)` —— **fillMaxWidth 先把宽度钉死成父容器宽度，后面的 max 形同虚设**，正方形按整个屏宽算高（1280dp+），整个弹窗比屏幕还高。改为 `sizeIn(maxWidth, maxHeight)` 在前 + `fillMaxWidth()` 在后，并把视口边长改为「窗口高度 − 260dp（标题/提示/比例条/按钮的预算）」、下限 200dp；
- 弹窗内容整体加 `verticalScroll` 兜底：极小屏/分屏下内容超高时可滚动，裁剪框手势不受影响（框内 pointerInput 自行消费拖动）；
- **数学公式以源码露出修复（平板截图坏例）**：模型最常把分段函数写成不带 `$$` 的单行 `\begin{cases}...\end{cases}`，环境补包裹逻辑只往后续行找闭合、单行永远匹配不到 → 整段落成 Markdown 纯文本（`\\` 被吃成 `\`、`&` 原样露出）。现在同行闭合的环境也自动补 `$$`（行内已带 `$$` 的不动）；顺带修复 `$$y(n) =`（`$$` 后带内容）开不了显示块、以及该场景下补包裹会产生两对 `$$` 错配的问题；新增 6 例回归测试。

## v1.0.6 (2026-09-07) · 方向恢复加强 + 气泡再放宽

- **拍完照仍卡在竖屏（v1.0.5 未解决）**：v1.0.5 的做法是「启动相机前 LOCKED，回来解除锁定交还系统」——实测在鸿蒙上无效，因为**解除锁定后系统会按它认为的当前方向继续显示，而那往往就是相机留下的竖屏**。改为记住拍照前的方向，回到前台时**显式强制**转回（`SENSOR_LANDSCAPE` / `SENSOR_PORTRAIT`），转屏动画结束后（1.5s）再交还 `FULL_USER`，不把方向写死；
- **气泡仍偏窄**：最大宽度从 `min(可用宽 × 0.86, 760dp)` 放宽到 `min(可用宽 × 0.92, 1400dp)`。760dp 在 1200dp+ 的平板横屏上只占六成，看着仍是"窄一条靠左"；现在基本铺满可用宽度，只留一点对侧留白区分收发双方（手机端手感不变）。

## v1.0.5 (2026-09-07) · 屏幕方向 / 平板横屏适配

三处与方向、横屏相关的 bug 修复：

### 1. 拍照被强制切竖屏、拍完不恢复（打卡拍照 / 饮食拍照 / AI 拍题）

- **根因**：`MainActivity` 未声明 `screenOrientation`，系统会按 `unspecified` 处理，平板等大屏设备上容易被套用「方向/信箱兼容模式」；同时系统相机普遍在 manifest 里声明竖屏，启动后会把屏幕带成竖屏，且**不受用户的方向锁定约束**——返回后部分 ROM（鸿蒙/EMUI、MIUI）不会把调用方 Activity 纠正回来，于是卡在竖屏；
- **修复**：
  - `AndroidManifest.xml`：`MainActivity` 增加 `android:screenOrientation="fullUser"`（明确支持四向，尊重用户锁定）与 `android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|fontScale|fontWeightAdjustment"`（旋转不再销毁重建 Activity）；
  - 新增 `ui/util/ScreenOrientationGuard`：启动相机前把方向锁在当前方向（`SCREEN_ORIENTATION_LOCKED`），`MainActivity.onResume` 回到前台时恢复 `FULL_USER`——系统按「顶层 Activity 的方向要求优先」裁决，相机退出后会自动转回我们锁定的方向；
  - 三处拍照入口（`CheckInDialog`、`MealScreen`、`AssistantScreen`）在 `takePicture.launch()` 前调用 `armBeforeExternalCapture()`；
  - 待回写的临时文件路径从 `remember` 改为 `rememberSaveable`（存路径字符串），旋转或进程被回收后重建也不会丢照片。

### 2. AI 助手旋转后布局错乱，必须重新进入才恢复

- **根因**：旋转会销毁重建 Activity（未声明 `configChanges`），重建期间「正在等相机返回」的临时文件与滚动位置丢失；同时回答正文是 `AndroidView` 里的 Markwon `TextView`，公式按「首次拿到的宽度」拆分，宽度变化后不会重新排版，于是沿用旧宽度拆出的超长公式，表现为内容溢出/错位，只有重新进入（重新创建 composable）才恢复；
- **修复**：
  - 上述 `configChanges` 让旋转只触发重组、不重建 Activity；
  - `MarkdownAnswer` 改为「测到真实宽度才渲染」：用 `Modifier.onSizeChanged` 记录 `TextView` 实测宽度，宽度/内容/配色变化时重新跑一次渲染（`LaunchedEffect` + 抽出的 `renderMarkdown()`），彻底去掉「用屏幕宽度兜底」的旧逻辑；
  - 裁剪弹窗 `PhotoCropDialog`：视口尺寸变化（旋转/分屏）时把裁剪框按比例迁移到新坐标系（`scaleRect`），不再是停留在旧位置的错位选框；预览图高度由写死 420dp 改为 `min(420dp, 屏高 × 55%)`，横屏下按钮不再被挤出屏幕；裁剪视口正方形边长按 `min(屏宽, 屏高 × 56%)` 封顶。

### 3. 对话气泡宽度不随平板变宽 + 公式溢出

- **根因**：气泡最大宽度写死 `widthIn(max = 480.dp)`，平板横屏可用宽度 900dp+ 时仍只有 480dp；公式可用宽度 `formulaMaxWidthPx()` 在 `view.width == 0`（首次渲染尚未布局）时用**屏幕宽度**兜底，平板上远大于气泡宽度，等于「不拆公式」→ 超出气泡被裁；
- **修复**：
  - 气泡最大宽度改为 `min(可用宽度 × 0.86, 760dp)`（用 `BoxWithConstraints` 取父容器实际可用宽度，比分屏下的屏幕宽度更准）：手机 400dp → 344dp 手感不变，平板横屏 1200dp → 760dp；
  - 公式宽度只认 `TextView` 实测宽度（减去 24dp 留白），拿不到宽度就先不渲染（最多晚一帧），不会再按屏幕宽度错误拆分。

## v1.0.4 (2026-09-07)

- 更新安装完成后自动清理旧安装包（只保留最新一个），释放存储空间；
- 百度网盘下载备份包显示实时进度条（百分比 + 已下载/总大小），下载慢时不再是「无响应」的空白等待。

## v1.0.3 (2026-09-07)

- 下载器修复：APK 保存目录与查找目录统一为 `files/Download/apk/`，并用 `COLUMN_LOCAL_URI` 精确定位下载结果 + 双目录兜底扫描，修复鸿蒙平板「下载完成后未找到 APK 文件」；
- 更新弹窗修复：版本号/大小/更新日志不再显示成 `${...}` 原始占位符；
- 下载失败可感知：DownloadManager 失败不广播，改为 5 秒轮询，失败后自动切换加速镜像重试（最多 3 源）。

## v1.0.2 (2026-09-07) · 英语背诵复习（SM-2 间隔重复）

- **背诵页**：墨墨式翻转自评卡片——正面只显示英文，默想释义后点卡片翻面，再选「认识 / 模糊 / 忘记」；顶部显示第几张与进度条，结束页汇总本轮三档数量；
- **SM-2 间隔重复**：新增 `Sm2Scheduler` 纯函数（`domain/english/`），间隔按 1 → 6 → 上次间隔 × 难度系数 递增；「模糊」间隔 ×1.2 并降低难度系数，「忘记」归零且 10 分钟后重来；难度系数下限 1.3；配套 8 个 JVM 单测；
- **每日新学上限**：默认 20 条/天，设置页「英语背诵」可滑动调整（5～100）；到期复习不限量；
- **数据层**：`english_entry` 增加 `review_due_at / review_ease / review_interval_days / review_reps / review_lapses / review_last_at`，数据库 v10 → v11 非破坏迁移（老条目自动成为新卡）；新字段自动纳入多端同步与版本化备份；
- **入口**：英语积累本顶部新增「今日背诵」卡片，显示待复习 / 可新学数量，一键进入背诵页（路由 `english/review`）。

## 2026-09-06 ～ 2026-09-07 · 应用内自动更新 + release 瘦身 + 开源

- **release 瘦身**：单 arm64-v8a + R8/资源压缩，APK 410MB → 284MB（OCR 模型 212MB 原样保留）；x86_64 仅 debug 构建保留（模拟器用）；
- **应用内自动更新全链路**：启动静默检查（仅 Wi-Fi / 每天一次 / 总开关三重节流）→ 发现新版本弹窗（版本号/大小/更新日志）→ 系统 DownloadManager 下载 → SHA-256 校验 → 提示覆盖安装；设置页「更新」卡片支持手动检查，无新版本显示「已是最新版本」，失败显示原因；
- **包名统一**：applicationIdSuffix 移入 defaultConfig，debug/release 统一为 com.example.lixing.debug，release 包可直接覆盖历史安装；
- **版本清单服务**：Cloudbase 云函数 update-check（普通事件函数，内联 JSON / update.json 直链 / 代理 GitHub API 三种清单来源，10 分钟缓存），发布脚本 docs/release.sh 一条命令出包；
- **开源整理**：目录 gitignore 规范化、MIT 协议、CHANGELOG 拆分。

## 2026-08-05 ～ 2026-09-05

### 2026-09-05 调试签名固定进项目 + 覆盖安装迁移说明

- **问题**：`C:\Users\<用户>\.android\debug.keystore` 在 09-05 凌晨被系统清理工具删除重建，导致新包与设备上已装版本的签名不一致，覆盖安装被系统拒绝；
- **根治**：调试签名改为**项目内密钥库** `app/debug.keystore`（公开口令的调试密钥，进仓库无泄密风险），`build.gradle.kts` 显式声明 `signingConfigs.debug`。今后用户主目录的密钥库再被清掉也不影响签名链，所有设备从 `LiXing-debug-20260905a.apk` 起统一到同一签名；
- **已装旧版的一次性迁移**：签名无法找回（私钥不存在的包无法互相覆盖），旧版需先在「设置 → 数据」导出 `.lixingbackup`（含数据库、照片、设置）→ 卸载 → 安装新包 → 导入备份；墨墨 Token、AI 密钥、WebDAV/百度凭据不进备份，需重新配置一次。


### 2026-09-04 WebDAV 多端同步收尾 + 公式渲染修复 + 仓库清理

- **WebDAV 多端同步全量落地**（设置页「多端同步」）：WebDAV 客户端（PUT/GET/PROPFIND/MKCOL/DELETE）、每设备云端变更文件、Lamport 时钟 + 墓碑的 SQLite 触发器、纯函数合并内核（字段级 last-writer-wins + 墓碑删除，双设备收敛有单测）、加密凭据存储、`SyncRepository` 编排（启动首同步 + 空闲 30 秒去抖自动同步 + 手动撞自动提示不排队）、仅 Wi-Fi 开关、同步统计 UI；14 个实体主键迁移为 UUID 并保持 v9→v10 非破坏迁移；照片列不参与同步；
- **同步恢复点**：手动同步（及首次自动同步）前自动创建 `before_sync` 恢复点，独立槽位保留 3 份；百度网盘「恢复成功并校验 SHA-256 后才允许删除云端旧副本」门禁；
- **公式渲染修复**（真机截图诊断）：多行推导出现「公式无法渲染」占位框的根因是超宽公式拆分把 `\begin{array}` 内部的 `\\` 当安全断点，切出孤儿环境头尾。修复：拆分点增加环境深度守卫（绝不在环境内部切）；超宽 `array`/`aligned` 按行解包为独立公式；跨行 `\left[…\right]` 与 `cases`/`pmatrix` 整块保留；
- **JLatexMath 单测解锁**：查明 JVM 单测中 `TeXFormula` 静态初始化失败的根因（Android 版库从 assets 读配置，需先 `JLatexMathAndroid.init(context)`，与 AWT 无关）；`Application.onCreate` 显式补 init；新增 6 例真实解析器回归测试（array 支持验证、清洗管线端到端、占位符路径），全量 **307 例测试 0 失败**；
- **确认按钮时有时无修复**：根因是长回答撞上输出上限把动作 JSON 截断、旧解析器只抢救正文丢弃全部动作。解析器升级为三级容错（整包 → 任意位置提取 → 截断抢救已完整条目），新增 4 例回归测试；
- **公式拆分第四种坏例修复**：`\,` 等符号命令里的逗号被当成逗号断点，切出以孤立 `\` 结尾的非法片段（占位框显示 `-y\,\mathrm dx+x\`，逗号留在下一段渲染成 `,dy`）。修复：符号命令（`\,` `\;` `\!` `\{` `\}` 等）与反斜杠作为整体跳过，顺带修正 `\{`/`\}` 转义花括号污染深度计数的问题，新增 2 例回归测试；
- **AI 生成保活**：回答生成期间启动 `dataSync` 前台服务 + 低优先级常驻通知（引用计数支持照片链路嵌套，结束自动停止），降低熄屏/切后台导致「没有任何输出」的概率；
- **路线图更新**：「跨设备会话同步」标记为已由 WebDAV 同步落地；
- **墨墨进度加入自动同步**：新增「跟随多端同步自动打卡」开关（默认关闭）。开启后每次同步会顺带拉取墨墨进度并打卡单词任务，采用「只增不减」策略（完成比例上升才推进，绝不撤销已有打卡与积分）、两次执行至少间隔 15 分钟、遵守仅 Wi-Fi、未配置 Token 静默跳过；打卡结果随任务表正常跨设备同步；
- **仓库清理**：删除 7 个历史 APK、`backups/`（1.9GB 历史快照）、`.tmp/`、`recovery/`、根 `build/`、云函数部署 zip 与构建日志，共释放约 4.9GB；安装包只保留最新版本。全量 **320 例测试 0 失败**；安装包 `LiXing-debug-20260904e.apk`。

### 2026-09-01 表格渲染 + 确认入口按钮 + 闪退兜底

- AI 回答支持 GFM 表格：引入 `markwon-ext-tables`，`| 命题 | 关键差异 | 结论 |` 这类内容渲染为真正的表格，不再是带竖线的纯文本；
- 计划修改 / 英语积累的确认入口改为挂在**产生该方案的那条回答气泡下面**的两个独立按钮（「📋 确认计划调整（N 项）」「📖 确认英语积累（N 条）」），不再回复一回来就自动跳到全屏确认页，可以先读回答再进入；没有待确认内容时按钮完全不渲染；入口唯一（删除了列表里的待确认卡片）；
- 修复「生成最终答案时小概率闪退」：JLatexMath 遇到模型输出的非法 LaTeX（缺括号、残留 `\tag`、aligned 前导非法字符等）会把 `ParseException` 包成 `RuntimeException` 在 UI 线程抛出导致闪退。现在给插件配置 `errorHandler`，单条公式失败画「公式无法渲染」占位；`MarkdownAnswer` 整体兜底，整段渲染失败回退纯文本并把异常与原文片段写入本机 `assistant-render.log`（≤200KB 自动截断）；
- aligned→array 转换加防御：嵌套环境 / `\hline` / 行数 > 12 / 列数 > 6 时不转换，宁可显示源码也不生成必然解析失败的 array；
- 单元测试回归通过；新安装包 `LiXing-debug-20260901.apk`。

### 2026-08-30 AI 助手自动写入英语积累 + 公式渲染兜底

- AI 学习助手新增英语积累动作协议：模型可通过 `english_actions` 返回 `ADD_ENGLISH_ENTRY` / `UPDATE_ENGLISH_ENTRY` / `DELETE_ENGLISH_ENTRY`，由对话输入、拍照识别或材料总结触发；
- 新增独立的「英语积累变更」确认页，逐条展示变更前/变更后与调整原因，支持逐项勾选、全部拒绝和接受所选，写入后以 Snackbar 汇总“新增 X 条，修改 Y 条，删除 Z 条”；
- 本地校验：类型（WORD/PHRASE/SENTENCE）、英文内容 ≤ 500 字符、释义 ≤ 2000 字符、修改/删除的 id 必须在库中真实存在；内容与现状相同的修改会被标记为不可应用；
- 英语积累上下文补上条目 id 并把上限提到 50 条，便于模型引用真实条目并避免重复新增；`EnglishEntryRepository` 新增 `get(id)`；
- 新增 `guardMissingEnglishActions`：用户明确要求写英语积累但模型没返回动作时，正文前会加上“本次没有写入任何内容”，不把模型的口头承诺当成执行结果；
- 系统提示词增补第 11 条协议，并明确英语条目删除是「不删除任何数据」的唯一例外；
- 公式渲染兜底：渲染前清洗 `\tag{}`、`\hspace{}`、`\vspace{}`、`[6pt]` 等 JLatexMath 不友好内容，并为落单的 `\begin{aligned}...\end{aligned}` 自动补 `$$` 并转换为 `array` 环境，修复小概率出现的公式显示为源码的问题；
- 移除 AI 助手顶栏的「英语积累」跳转按钮（英语积累的手动入口仍在今日页与专注页，AI 写入走新的动作协议）；
- 单元测试：`AssistantResponseParserTest` 新增英语动作解析与非法动作跳过的用例；`AssistantModelClientTest`、`AssistantResponseParserTest` 均通过。


### 2026-08-11 AI 拍题管线重构与公式渲染兼容

- AI 今日临时计划支持逐项“仅今日跳过”：用户说今天不做或删除今日任务时，助手生成非破坏性的跳过方案，不删除记录、不改模板和未来安排，也不计漏卡；
- AI 今日请假新增单条 `TAKE_TODAY_OFF` / `KEEP_TODAY_TASKS` 批量协议，避免任务较多时逐项动作导致结构化 JSON 截断；整日请假本地校验月度额度，缺少合法动作时明确提示没有修改数据；
- AI 学习助手新增独立的「计划修改方案」确认页，按仅今日/长期计划分区展示修改前后，支持逐项选择、全部拒绝和接受所选；今日临时调整可改目标量或移动时段，但不会改任务模板与未来任务，接受前仍自动创建 `before_ai_apply` 恢复点；
- 拍题路由重构：「多模态模型」开关开启时照片直发模型识题作答（EXIF 方向校正 + 最长边 2048 压缩）；关闭时走本地数学 OCR 预览，本地 OCR 明确定位为纯文本模型兜底；
- 本地数学 OCR 增强：Pix2Text MFD 对宽幅/窄幅图自动重叠分块；MFR 改为与官方预处理一致的 384×384 直接缩放；ML Kit 全文兜底，公式结果只替换通过质量门控的非中文区域，低置信/幻觉公式自动回退普通文字；表格经网格线检测输出 Markdown；
- 回答公式渲染兼容：`$...$`、`\(...\)`、`\[...\]` 归一化为 `$$...$$`，解决部分模型公式显示为源码的问题；代码块、行内代码与金额不误转换；
- 思考强度按模型配置可调（低/中/高，默认低），答案截断时自动续写一次；
- 长公式自动换行：JLatexMath 实测宽度，超宽公式在顶层安全断点拆行、超宽行内公式转块级，解决右侧溢出裁剪；
- AI 设置新增「拍题识题模型」与「饮食校准模型」两个多模态默认选择；纯文本模型拍题时先由识题模型转写进可编辑预览，本地 OCR 退为兜底；饮食拍照可由校准模型估算名称/克重/营养，失败回退端侧模型；
- 拍题交互简化为“发照片直达回答”：转写/本地 OCR 在后台静默完成，不再弹出识别进度与可编辑预览（多模态直送、识题模型转写、本地 OCR 三条路径均直接进回答流）；
- 拍题裁剪放宽：最小裁剪尺寸由 72dp 降为 24dp，单行长题可以裁出很扁的框；比例预设新增 3:1 横条；
- 真机回归方式：日常设备拒绝安装测试组件，改为仅 debug 构建的一次性诊断 Activity 读图写结果，入口用完已移除，正式包不含；
- 现状说明：百度网盘下载尚无前台服务与断点续传，熄屏/切网可能中断下载（`Software caused connection abort`），需保持前台重试；联网搜索已接入 DeepSeek（Responses API）与小米 MiMo（Chat Completions web_search 工具）两种服务端内置搜索。

### 2026-08-09 AI 学习助手（可选联网）

- 新增 AI 学习助手页：对话问答、可选上下文、快捷提问；默认关闭；
- 今日页任务卡在副标题行直接显示学科名（学科色），无需再靠色点猜学科；
- 助手可返回四类计划修改建议（调整时段/模板、新增模板、调整今日目标），需逐条勾选确认，应用前自动创建恢复点；
- 采用 App 直连 OpenAI 兼容接口：设置页配置接口地址、模型名与 API 密钥，可任意切换 DeepSeek / 月之暗面 / 智谱等服务商，无需部署或修改云服务；
- API 密钥用 Android Keystore 加密，不进版本备份；
- 新增动作协议解析与计划修改执行器单元测试；`cloudbase/ai-assistant/` 保留为可选的自建网关方案。
- 上下文改为「模型自动预判 + 手动勾选」：发送时先让模型按问题选择需要附带的本机数据，手动勾选的始终附带；
- 修复墨墨 get_today_items 请求携带显式 null 被服务端拒绝的问题（explicitNulls = false）；
- 今日页右上角精简为「饮食 / 回顾」，英语积累与 AI 助手入口移至专注页统计下方；
- AI 会话持久化：新增 `assistant_conversation` / `assistant_message` 表（v6→v7 非破坏迁移，纳入版本备份），助手页右上角「历史对话」可回看续聊、逐条删除；计划建议仍只在内存中逐条确认。
- 助手新增拍题：多模态开关决定「直接发图」或「本地 ML Kit OCR 提取文字后发送」，OCR 完全离线；识别不到文字会提示。
- 拍题支持相机连续拍摄和相册多选（最多 9 张），修复 OCR 正文未进入模型请求的问题；输入区跟随键盘上移，消息显示可点击放大的图片缩略图；AI 图片消息与图片文件纳入会话历史和版本化备份（数据库 v7→v8）。
- AI 设置支持保存、编辑、删除和切换多套 OpenAI 兼容模型配置；每套配置独立记录 API 密钥与多模态能力，旧的单模型设置会自动迁移为第一套配置。
- OCR 正文改为只发送给模型，聊天气泡只显示用户主动输入的文字和图片缩略图；数据库升级至 v9，通过非破坏性 v8→v9 迁移增加可空的界面展示字段，旧消息继续按原样显示。

### 2026-08-09 英语积累

- 今日页新增“英语”入口；无学习计划时同样可以进入；
- 新增本地英语积累本，可录入单词、短语、句子及释义，支持搜索、分类筛选、编辑和删除；
- 数据库升级至 v6，采用非破坏性 v5→v6 迁移，`english_entry` 已纳入本机/网盘版本化备份；
- 新增 DAO 搜索筛选、迁移保留旧数据及备份恢复单元测试。

### 计划与今日页

- 修复计划时段时间看似可编辑但无法正确写入的问题；可编辑时间统一经过严格解析并兼容全角字符；
- 修复 `21:35-22:15` 等有效时间被误判无效的问题；
- 新增时段默认时间改为 `00:00-00:00`；
- 计划修改后立即重新协调当天已物化任务，今日页无需退出重进即可更新；
- 即使时段暂时没有任务，今日页也保留对应时段卡片，新增“睡前”等时段后可见。

### 专注计时

- 修复番茄钟无法检测切出 App 的问题；专注进行中切到其他应用会记录待提交中断；
- “放弃本次”不写入中断次数；只有用户接受并记录本次计时后，时长与中断次数才一起提交。

### 版本化备份与多端共享

- 新增完整 `.lixingbackup` 格式：Room 全表、非敏感设置、照片、清单、数据库版本和 SHA-256 校验；
- 支持本机版本轮转、系统文件选择器导入导出、恢复前 `before_restore` 自动恢复点；
- 新增百度网盘 OAuth、Android Keystore AES-GCM 凭证加密，以及分片上传、版本列表、下载校验和确认恢复；
- OAuth 中转由受阻的 Cloudflare Workers 迁移至腾讯云 CloudBase，已在真机验证授权、上传、列表、下载与恢复确认全链路。

### 自我承诺到期结算

- 补齐承诺功能：此前 `commitment` 表、枚举、DAO、幂等键已在 v2 schema 中就位，后续已通过 v3→v4 迁移增加多维度指标和自定义奖励；
- 新增 `domain/settle/CommitmentSettler`（纯函数）：结算判定 + 进行中实时进度，百分比向下取整保证显示与结论自洽；
- `GamificationRepository` 新增承诺读写、进度查询与 `settleExpiredCommitments()`；
- 结算挂在 `SettleDayUseCase.onAppStart()` 与每日 Worker，均排在当天结算之后（顺序由 `onAppStart()` 内部保证）；
- 「我的」页新增立约对话框、进行中卡片（含实时进度条）与历史回看；承诺进度跟随今日 `day_record` 变化刷新，打卡后立即可见；
- 到期结果通过今日页弹窗逐条回看，连带当初写给自己的那句话；
- 新增承诺相关单测，调试 APK 位于 `app/build/outputs/apk/debug/app-debug.apk`。

### 2026-08-06 反馈修订

- 锁屏/熄屏不再误计为番茄钟切出中断；
- 每个时段可设置最低完成数，最低数全部完成为 100%，超额完成允许超过 100%；
- 首页完成度环、周报/月报及时段统计采用新的时段完成口径；
- 墨墨累计背诵时长按增量计入英语投入，避免重复同步累加；
- 阶段性立誓新增累计专注分钟、达标天数和自定义奖励；数据库升级至 v4；
- 年度热力图改为实时观察 `day_record`，与自定义学习日起点使用同一学习日，并按最近 12 个自然月逐月翻页。

### 文档修正

- 路线图第 3 项「今日页已完成任务的撤销入口」实际早已实现（`CheckInUseCase.revoke()`、`CheckInDialog` 撤销按钮、积分按流水回滚），本轮从待办中移除。

