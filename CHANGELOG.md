# 更新日志

> 按时间倒序记录功能变更。

## v1.0.39 (2026-09-16) · 公式定界符、坐标轴箭头、思考窗口位置、表格按需撑宽

四个用户反馈问题的修复。前三个是明确的 bug，第四个（表格宽度）根因坐实后一并改掉。

### ① `\lvert … \rvert` 渲染失败（`⚠ 公式无法渲染 (ParseException)`）

先写**能力探测**测试实测 JLatexMath 到底支持哪些定界符，而不是凭印象猜。结果是**反直觉的大小写不对称**：

| 写法 | 结果 |
|---|---|
| `\lvert…\rvert`（单竖线） | ❌ ParseException |
| `\left\lvert…\right\rvert` | ❌ |
| `\lVert…\rVert`（双竖线） | ✅ |
| `\|` / `|` / `\left|\right|` | ✅ |
| `\lbrace` `\langle` `\lceil` `\lfloor` | ✅ |

于是降级规则**按语义分层**，而不是一律换成单竖线：

- `\lVert` / `\rVert` → `\|`（**保留范数语义**）
- `\lvert` / `\rvert` → `|`（绝对值 / 模）

⚠️ 这里有个比渲染失败更危险的陷阱：若把 `\lVert` 也降成单竖线，**范数会被写成绝对值**——「渲染成功但数学含义错了」比「渲染失败」更糟，因为没人会发现。专门用 `doubleBarIsDowngradedToNormNotAbsolute` 守着这条。

**本轮最隐蔽的坑**：`Regex.replace(input, "\\|")` 会把替换串里的反斜杠**再解析一次**当转义符吃掉（走的是 Java `Matcher.replaceAll` 语义），结果出来是字面 `|`。铁律：**凡替换串含反斜杠，一律用 lambda 形式 `{ "\\|" }`**（返回值是字面量，不经转义解析）。文件里既有的 `\color` / `\intertext` / `dcases` 替换也已统一改成 lambda 形式。

### ② 画图像坐标轴头部改成箭头

原来是端点一条小垂线（像刻度），现在改成两条斜线收敛于尖端，张开半角 `atan(0.5) ≈ 26.6°`。

尺寸**按画布宽高比例**而不是 dp——画布是物理像素、dp 会随 density 放大，这是 v1.0.32 就踩过的坑。纵臂从 0.013 调到 0.017 才和横臂视觉重量一致，这个差异是靠 3.5× 放大对比图（`docs/prototype/`）才看出来的，光看整体图分不出。

测试用**记录型 Canvas** 捕获斜线，新增 3 项断言：是箭头不是端刻度、**density 1/2/3 下臂长必须相同**（守住「不能用 dp」这条铁律）、指向与轴线方向一致。

### ③ 思考窗口挪到最新输出气泡下方

现象：复杂题目「回复一段之后还在继续思考」时，思考窗口停在**页面最上方**。

根因：消息列表是 `reverseLayout = true`（从底部往上排），原先作为**独立 LazyColumn item** 的思考面板会被排到整列**最上方**。而流式回答是往最新气泡里不断追加内容的——气泡往下长，面板钉在顶部不动。

修法：把面板挂进 `index == 0` 那条消息的 Column 内、排在气泡之后，视觉上正好是「紧贴最新输出气泡下方」，随输出一起下移。同时**合并原先分成两个分支的处理**（原来只有 `role == "user"` 才挂面板），统一为 `index == 0 && state.busy`——这正是「位置时对时错」的来源。另补空列表兜底：ViewModel 是**先置 `busy=true` 再写消息**，中间存在一帧空列表。

### ④ 表格按需撑宽（不再是「无条件 1.8 倍」）

用户反馈：「表格有些时候宽度富裕很多，但是依然控了很大」。用户进一步确认：**窄表格（两三列短文字）也会被撑宽**。

根因坐实：`tableWidthPx = containerWidthPx * TABLE_WIDTH_FACTOR`，**没有任何「够用就停」的分支**。设计初衷（让窄气泡里的单元格不被压成一团）没错，但缺了收敛条件——两三列短文字本来 0.5 倍气泡宽就够，也被拉到 1.8 倍，右侧一大半被顶到屏幕外，于是「看不见内容」。

改为：先估算表格自然宽度，**放得下就不撑，放不下才撑到刚好够**，1.8 只是上限（改名 `TABLE_MAX_WIDTH_FACTOR`）。

两个关键决策：

1. **判据是「最宽列 × 列数」，不是「各列之和」**。Markwon 把可用宽度**均分**给每一列，不按内容分配。三列 200/50/50 的表格，按「和」是 300 看着正好，实际均分后每列只有 100，第一列照样被挤换行。
2. **用纯文本估算，不用试排**。估算在流式期间会被反复调用，试排意味着每帧一次 `setText` + `measure`（一次就是一次完整文本排版）——那正是「长回答里表格拖不动、图点不开」的可疑根因。纯文本估算只有微秒级开销。行内公式按源码长度保守估并给下限，宁可多撑一点也不把公式挤变形。

新增 `TableWidthEstimateTest`（**13 例全绿**）：放得下不撑 / 撑到刚好够不顶满 / 超上限截断 / 窄表格不撑宽 / 宽表格要撑 / 均分判据（200/50/50 与 200/200/200 宽度相同）/ 分隔行不参与计算 / 公式不估窄 / 强调标记不占宽 / 异常输入不崩。

⚠️ 又踩到 Robolectric 的假值：`measureText` **直接返回字符数**（实测「名称」= 2.0），因为它压根不栅格化字体。所以把测宽函数做成**可注入**的（默认 `paint::measureText`），测试注入确定性实现，「按需撑宽」这个行为才验得动。

### 记录但本版未修

- **长气泡靠后位置表格拖不动 / 图片点不开**：已找到一条能同时解释三条症状的新嫌疑——高度守望循环把主线程榨干（`measuredHeightCompat()` 是 `measure()` 而非读缓存，且 12 秒 deadline 因 `LaunchedEffect` key 含 `content` 而永不到期）。已给出真机验证法（生成完等 15 秒再拖）。详见 `docs/known-issues.md` #A/#B。
- **`**加粗**` 看不出效果**：渲染管线已用真实 Markwon + AST 探针证明**完全正常**（AST 有 `StrongEmphasis`、span 是 `StrongEmphasisSpan` 而非 `StyleSpan`、覆盖区间精确、7 项边界全绿）。问题只可能在字体层或内容层，需提供原始文本才能定论。详见 `docs/known-issues.md` #C。

## v1.0.38 (2026-09-15) · 纵向不再拉成「深碗」——把「纵轴留白」写进提示词，渲染器一行未动

用户反馈：横向已经没问题了，但**纵向还是不对**——「按照实际图像的比例来看，这个曲线应该曲率相当的小，如果按照其真实曲率高度的话，可能看起来和矩形无异，但是现在这个曲线太深了，严重干扰了真实的曲线形状」。用户进一步澄清意图：这道题本身没说明 f_c（载波频率）与 B（带宽）的大小关系，「**真正有意义的是我在 APP 里搜这道题的时候我接入 API 能不能将这个图画到一个合适的样子**」——问题不在渲染层，而在**模型给出的纵轴范围**。

### 先算清楚「真实曲率」是什么，避免顺着「压成矩形」改错题

$y = 4\pi^2 N_0 (f^2 + f_c^2)$ 的碗深只由 $f_c/(B/2)$ 决定。实测：比值 0.25× 时碗深 94%、1× 时 80%、2× 时 50%、4× 时 6%。也就是说，

- 「看起来和矩形无异」**只在 $f_c \gtrsim 4\times(B/2)$ 时成立**；
- 若两者同量级，真实碗深本来就是 50%~80%，**画成平缓的横线反而是画错了**。

结论：**不能**让渲染器统一压扁曲线——那会在 $f_c$ 与 $B/2$ 同量级时把正确的碗压平。

### 方案选型：只改提示词（方案 A），渲染器不动

给出三个方案并推荐 A：

| 方案 | 做法 | 问题 |
|---|---|---|
| **A（选中）** | 只改提示词，让模型自己给合适的 `y.min/max` | 依赖模型遵守，但语义正确 |
| B | 渲染器统一把曲线压到画布 31.9% 高 | **压两次**（y 范围留白 + 内缩留白），且模型给的范围失去意义；$f_c$ 与 $B/2$ 同量级时会画错 |
| C | 渲染器按条件兜底 | 引入「什么样的曲线该压」这种渲染器无法判断的语义决策 |

**决定性理由**：纵轴留多少白是**语义决策**，取决于题目意图——模型知道 $f_c$ 与带宽的关系，渲染器不知道。上一轮把 `PLOT_INSET_Y_RATIO` 从 `0.07` 改成 `0.03` 方向就是错的：横向内缩的目标是「主动留白」，**纵向的目标只是「防贴边」**，0.03 让曲线吃掉了画布高度的 63%（参考图仅 31.9%）。本轮**渲染器保持 `PLOT_INSET_Y_RATIO = 0.03f` 不动**，改从源头解决。

### 落地：`SYSTEM_PROMPT` 画图指令 6 处改动

第 13 条画图指令的**真根因**是：它只说了「范围要**覆盖**主体特征、别**裁掉**峰值」，**从没说「别贴满」**；而且三个 JSON 示例里的 `y` 清一色只有 `label`/`ticks`，**一个 `y.min/max` 的例子都没有**。模型照着示例学，自然只给 `label` ⇒ 客户端 `balancedRange` 按数据实际跨度自动求范围（只留 6% 边距）⇒ 曲线铺满整个高度，被纵向拉成深碗。

- **示例补上 `y.min/max`**：`{"y":{"label":"S(f)","min":0,"max":15}}`，并跟一句说明「只给 label 不给 min/max 是错的，客户端会按数据自动求范围、把曲线铺满整个高度」；
- **拆掉 x/y 方向的矛盾表述**：原句「范围太宽形状会被压平」说的是 x 轴，紧挨着 y 轴的「覆盖主体特征」会**误导模型把 y 也收紧**。改为「**x 与 y 要分开对待**」——x 轴收在「主体特征 + 少量余量」，y 轴反过来**要留白**；
- **原「x 与 y 的范围都要覆盖主体特征本身」拆开表述**：x 覆盖主体特征、y 包住峰值，两者都不许裁数据，y 的留白另说；
- **新增核心条目——纵轴留白规则**：「曲线只占纵轴的约 1/3」，并给出可直接执行的**×3 口诀**：`y 范围 ≈ 数据跨度 × 3`（峰值 100 谷值 20 ⇒ `"y":{"min":0,"max":260}`；峰值 5 谷值 1 ⇒ `"y":{"min":0,"max":13}`），下限一般取 0；
- **新增：留白后纵轴最好别标刻度**（标了会让读者以为上面那片空白有含义），用 `"y":{"label":"S(f)","grid":false}` 只给轴名；
- **新增：题目没给大小的参数不要擅自定**——f_c 与 B 的关系若题目未说明，按物理常识处理（载波频率远高于带宽 ⇒ f_c² 项主导 ⇒ 曲线接近平缓横线），不确定就用 ×3 留白，**宁可平缓也不要拉成深碗**。

### 可测性改造与回归测试

`SYSTEM_PROMPT` 从 `private companion object` 改为 `internal companion object`（`versionName`/`SYSTEM_PROMPT` 均为 `internal const val`），使其可被同模块测试直接断言。

新增 `AssistantPlotPromptTest`（**8 例全绿**）：① ×3 规则与「1/3」「深碗」关键表述齐全；② 明确交代了「省略纵轴范围 ⇒ 自动求范围 ⇒ 铺满」的因果；③ 用正则断言**示例里确实示范了 `y.min/max`**（`"y":{[^}]*"min":[^}]*"max":`）；④ x/y 分开表述；⑤ 留白后刻度处理；⑥ 未给参数按物理常识处理；⑦ **反面断言**——旧的「x 与 y 的范围都要覆盖主体特征本身」必须不再存在；⑧ 画图协议的既有关键约束（不做任何外扩、刻度用符号、`\frac{B}{2}`、`[[FIGURE:1]]`、最多 6 条曲线）仍然完整。

**提示词也能写回归测试**：`SYSTEM_PROMPT` 是纯字符串，改错**编译不报错、运行不崩**，只会让模型产出悄悄变差——这类「静默退化」正是最需要断言兜住的。

**反向验证成立**：把 6 处新增内容还原后强制重跑（`--rerun`），**8 项里 7 项变红**（第 8 项「既有约束」按预期保持绿），证明测试确实在守着这些行为。

### 选型的量化证据

用 Java2D 复刻渲染常量，把**同一份数据**（$f^2+0.25$，模拟 f_c 与 B/2 同量级）放进四种 y 范围并列对比：

| y 范围 | 曲线占画布高 | 观感 |
|---|---|---|
| 模型没给 range（自动 +6%） | 89% | 深碗，现状 |
| `[0, 峰值]`（刚好包住） | 100% | 最糟，顶死上下边 |
| `[0, 峰值×2]` | 50% | 仍偏深 |
| **`[0, 峰值×3]`（提示词推荐）** | **33%** | **与参考图 31.9% 几乎一致** |

同时新出 `PlotRefDepth` 四档纵向内缩对比（`insetY` = 0.03 / 0.15 / 0.20 / 0.2618），把「现状 63% vs 参考图 31.9%」精确定位——这组图是方案 B 的反面证据：即便把 `insetY` 调到 0.2618 能凑出 31.9%，那也是**用几何掩盖语义问题**，模型给的范围依然被无视。

### 附带修复：JDK 版本导致的 8 项测试假红

全量测试首次跑出 8 个 failure，全是 `NoClassDefFoundError: Could not initialize class org.scilab.forge.jlatexmath.TeXFormula`（`JLatexMathArraySupportTest` 7 项 + `JLatexMathLeadingTokensTest` 1 项），两个能力探测输出也变成 109/109 全 FAIL。**根因不是代码**：构建脚本落的 JDK 是 25，而 JLatexMath 在 JDK 25 上静态初始化失败，且**失败是粘性的**（一旦初始化失败会污染整个 JVM）。显式 `JAVA_HOME` 指向 JDK 21 后 **512 tests / 0 failures**。

新增 `JLatexMathAvailabilityTest`，把 `JLatexMathAndroid.init()` 的异常**原样抛成 AssertionError**，下次再遇到环境问题时能直接看到根因而不是一串 `NoClassDefFoundError`；并在 `app/build.gradle.kts` 与三个受影响测试类的 KDoc 里写明 JDK 21 要求。

## v1.0.37 (2026-09-16) · `cases` 公式恢复渲染 + 曲线只占横轴约 70% + 靠后的图终于点得开

用户反馈：① 「4. 最终结果」里的分段函数公式**没有渲染**——等号及之前的部分是公式，`\begin{cases}` 及其后内容却**以 LaTeX 源码原样露出**；② 生成的图像**曲线占满整个画出的横坐标范围**，「其实曲线占画出横坐标 70% 左右就最好最美观，而不是将画出的横坐标范围顶满」，并以教材题 3.3(c) 的图为参照；③ **长回答气泡靠后的图仍然点不开**。

- **公式没渲染的真根因：`$$` 显示块定界符被多补了一对，把显示块劈成了两半**。`convertAlignedEnvironments` 负责给「模型写了数学环境却忘了用 `$$` 包起来」的情况补定界符，它的 `alreadyWrapped` 判据过去只看两件局部的事——「上一行是不是以 `$$` 开头」「环境之后是不是紧跟 `$$` 结尾」。于是当模型把公式写成「`=` 收尾换行 + `\begin{cases}` 单独起一行」时，环境**明明已经在一个打开着的显示块内部**，却被判定为「没被包裹」，于是**在已打开的块里又插进一对 `$$`**。一对多余的定界符把原本完整的显示块切成两段：前半段（`S_{Y_c}(f) = … =`）照常渲染成公式，后半段（`\begin{cases}` 起始的部分）掉出了数学上下文，被当成普通文字按源码原样画出来——与用户截图完全一致；
  - **诊断过程刻意先证伪、再定位**（本轮三次探测，每次都推翻了前一个假设）：
    1. 先怀疑「JLatexMath 不支持 `cases`」——写了能力探测测试，把 15 条 `cases` 变体（含截图里那种畸形写法）逐条喂给真实 `TeXFormula`，**15/15 全部通过**，假设被推翻；
    2. 再怀疑「清洗管线把环境拆散了」——逐步 dump 发现 `cases` 在标准 `$$` 块内始终完整，假设再次被推翻；
    3. 最后逐行 dump + 统计 `$$` 收支，才发现 **`sanitizeAssistantLatex` 把 2 个 `$$` 变成了 4 个**，元凶正是 `alreadyWrapped` 的局部判据；
  - **修复**：新增 `countDisplayDelimiters()`，从文档开头到环境起始处逐行统计 `$$` 的出现次数（**跳过代码围栏**，围栏内的 `$$` 不算），**奇数即表示「正处在一个已打开的显示块内部」**。`alreadyWrapped = insideOpenDisplay || legacyWrapped`，两种写法都能正确识别，且**只影响原本就会走到补定界符分支的场景**，标准写法零回归；
  - **测试**：新增 `CasesDelimiterTest`（4 例）——① 等号后换行的 `cases` 不会被补出多余的 `$$`（断言定界符数量守恒、且 `=` 与 `\begin{cases}` 之间不出现 `$$`）；② 裸 `cases` 环境（模型漏写定界符）仍会被正确包裹；③ `aligned` 环境在已打开的显示块内也不会被重复包裹；④ 代码块里的 `$$` 不参与配对统计。

- **曲线占满横轴的修正：横向内缩由 5% 提到 15%，曲线占绘图区约 70%**。v1.0.36 已经把「坐标轴范围（语义量）」与「绘图区物理边界（视觉量）」解耦，并引入 `PLOT_INSET_X_RATIO = 0.05f`。但那只是让曲线不再顶死在框线角上，曲线仍占 90% 宽，用户依然觉得「占满了」。本轮以**教材题 3.3(c) 的观感**为基准收敛：
  - `PLOT_INSET_X_RATIO` 改为 **`0.15f`**（`PLOT_INSET_Y_RATIO` 保持 `0.07f`）——纵向本来就有轴标题与刻度占位，不需要再加；
  - 选型时用 Java2D 复刻渲染常量出了 **0% / 5% / 15% / 20% 四档对比图**，实测 15% 时曲线正好占绘图区 70%，两侧各留 15%，与教材图一致；
  - 复核：`drawW=872.2`，`B/2` 竖线落在 `x=1173.9`，左右对称误差 `0.00px`，曲线外缘到画布右边距 `186.9px`（画布宽的 13.35%）；
  - 这是本项目第三次调这个比例（0% → 5% → 15%），注释里已把三档的取舍与用户原话一并记下，避免以后再来回摇摆。

- **「靠后的图点不开」的真根因：`setMovementMethod()` 会把 clickable/longClickable 重新打开，我们关属性的时机在它之前，等于白关**。v1.0.36 认定根因是「漏关了 `isLongClickable`」，于是补上 `isLongClickable = false`。但那三行属性的关闭**写在了 `setMovementMethod()` 之前**，而 AOSP 的 `TextView` 是这么写的：

  ```java
  public final void setMovementMethod(MovementMethod movement) {
      if (mMovement != movement) {
          mMovement = movement;
          ...
          fixFocusableAndClickableSettings();   // ← 就是这里
      }
  }
  private void fixFocusableAndClickableSettings() {
      if (mMovement != null) {
          setFocusable(FOCUSABLE);
          setClickable(true);        // ← 又把 clickable 打开
          setLongClickable(true);    // ← 又把 longClickable 打开
      } else { ... }
  }
  ```

  也就是说：**只要挂了 MovementMethod，框架就会立刻把这三个属性重新打开**。我们「先关属性、后挂 MovementMethod」，前脚关掉后脚就被框架翻回来 ⇒ **v1.0.36 那条修复实际上从未生效**，这正是用户「靠后的图还是点不开」迟迟不愈的原因。（此前测试在 `!view.isClickable` 上稳定变红，不是测试环境失真，而是如实反映了这个真实缺陷。诊断方式：用 `javap` 反编译 Markwon 各插件确认没人动这些属性，再反编译 Robolectric 的 android-all jar 拿到 AOSP 真实实现 —— SDK 里的 `android.jar` 只是抛 `Stub!` 的桩，查不出行为。）

  **修复从两道一起下手**：
  1. **顺序反转**——先 `movementMethod = LinkMovementMethod.getInstance()`，**再**把框架顺手打开的三个属性关掉；
  2. **真正不依赖属性的兜底——重写 `onTouchEvent`**，只在**触摸点确实落在 `ClickableSpan` 上**时才消费，其余一律返回 `false` 放行。判定沿用 AOSP `LinkMovementMethod` 的同一套坐标换算（减去 `totalPaddingLeft/Top`、加上 `scrollX/Y`，再 `getLineForVertical` / `getOffsetForHorizontal` 反查字符偏移），**任何越界或异常都返回 `false`**——判不出来时宁可让链接偶尔点不到，也绝不吃掉下方图片的点击。这一层由 View 自身裁决，任何插件之后重新 `setMovementMethod` 都翻不动它；
  - **测试**：`MarkdownChunkInteractionTest` 扩到 3 例——① 创建后不抢占触摸；② **跑一遍真实的 `setMarkdown` 渲染之后**仍不抢占（新增，专治本条——只在创建后检查会漏掉框架翻盘的整条路径）；③ 表格块（`selectable = false`）不抢占触摸且刻意不给 MovementMethod。

## v1.0.36 (2026-09-16) · 曲线不再溢出定义域 + 纵轴刻度文案不再变成「…」+ 四周留出视觉余量

用户反馈：① 上次修完「左侧文字挡住图像」之后，**新出现生成该图时曲线稳定溢出它应有的 −B/2 到 B/2 之间**（参考教材原图，曲线应正好终止在 ±B/2 的虚线上）；② **左边稳定显示三个点**（纵轴刻度只剩一个「…」），「略减小字体或者加宽左边空间都可以」；③ 修完之后**曲线边界恰好就是整个 plot 的边界**，「既不直观、无法看出曲线特征，也不美观」，**既要修好边界，又要给曲线边留出较合适的余量**。

- **曲线溢出 ±B/2 的真根因：x 轴坐标范围把「定义域」当成了「视野」去外扩**。v1.0.34 为了解决「曲线首末点贴在框线上、看起来像被切断」，给 `conservativeRange` 加了「数据贴边就外扩 4%」的规则。但这两件事的语义完全不同：
  - 模型**显式给了** `x.min/max` 时，那是**定义域**（题目说「理想低通/带通谱只在 |f| ≤ B/2 上有定义」，模型据此把范围设成 ±B/2、曲线表达式也只在 ±B/2 内有效），**外扩它就是错的** —— 范围变成 ±2.08 之后，±B/2 的 `markLine` 竖线落到绘图区**内部**（离边框留着 4% 的缝），而曲线恰好终止在那条竖线上，于是视觉上就成了「曲线越过了定义域边界继续往外延伸」；
  - 模型**没给**范围时，那才是纯粹由数据推断出来的视野，此时留白是对的 —— 问题是「曲线首末点压框线」，应该在**推断侧**解决；
  - 现在按此分层：**模型给了合法且能覆盖数据的区间 ⇒ 原样采用、绝不外扩**；只给了单侧就只推断另一侧；完全没给才两侧都留白。同时定义域**不允许裁掉数据**（模型偶尔会给比实际曲线更窄的窗口，此时必须扩展，否则曲线被无声截断）；
- **纵轴刻度只剩「…」的真根因：长公式文案被硬夹在固定宽度的左侧留白里**。左侧留白是按画布宽固定取 8.2% 的，而参数化刻度文案（`N₀(2πf_c)²`）用真实字体量出来需要约 112px、可用宽度只有 71.5px ⇒ 走 `planLabelLayout` 的截断分支，一个片段都放不下，最后只画了一个省略号「…」——正是用户看到的「三个点」；
- **修复改成两段式自适应（缩字号优先，加宽留白兜底）**：新增纯函数 `planYTickSize`，在**绘图区定位之前**先按基准字号量出最宽的那条刻度文案，
  1. 装得下 ⇒ 沿用基准字号；
  2. 装不下 ⇒ **先按宽度比例压缩字号**（下限为基准的 62%，实测 112px 文案缩到 15.2px 正好落在原有 81.4px 留白内，**不侵占绘图区宽度**）；
  3. 缩到下限仍不够 ⇒ 才把左侧留白适当加宽（上限为画布宽的 10%）。
  两条路的结果都是**完整文案**，省略号不再是常态；夹取逻辑保留为最后兜底；
- **提示词同步**：明确告诉模型「客户端会把 min/max 当作定义域原样使用、不做任何外扩」，因此既不要把定义域写宽（会多出无意义的空白），也不要让曲线铺不满给定的范围；
- **第三轮修正 —— 修过头了：坐标轴范围与绘图区边界必须解耦**（用户第 ③ 条反馈）。把定义域精确到与绘图区边框重合之后，曲线两端就**顶死在框线的角上**，既看不出「曲线在这里被定义域截断」这件事，视觉上也挤得难受。问题在于此前把两个语义完全不同的东西当成了一个：
  - **坐标轴范围是语义量** —— 它决定 `x` 值映射到哪个像素、刻度标在哪、`markLine` 落在哪里。它**必须精确**，`−B/2` 就得映射到定义域左端点，任何外扩都会导致曲线越过边界；
  - **绘图区物理边界是视觉量** —— 它只决定「画在哪块矩形里」。曲线并不需要铺满这块矩形；
  - 于是**范围保持精确、绘图区向内缩**：新增 `PLOT_INSET_X_RATIO = 0.05f` / `PLOT_INSET_Y_RATIO = 0.07f`（按绘图区宽高取比例，**不用 dp** —— 画布是物理像素，dp 会随 density 放大而压平绘图区，这是 v1.0.32 的真根因）。所有落笔内容（markArea、网格、裁剪区、坐标轴、断轴、刻度位置、轴标题、markLine）改走内缩后的 `drawX/drawY/drawW/drawH`；图例仍用外层矩形（它本就位于绘图区上方的标题带里）；
  - 效果：`±B/2` 的虚线**落在绘图区内部**，曲线终止在虚线上、四周各留约 4.5%（横）/ 5.5%（纵）的空白，与教材原图的观感一致；
  - **测试**：`定义域边界位置精确且与框线留有可见余量`（记录型 Canvas 读实际绘制坐标）断言边界线距外层框线恰好为 `insetX`（±1.5px）、余量落在画布宽的 2%~12% 区间、左右对称 —— 同时锁住「位置精确」与「确实有余量」两个要求，避免以后任何一侧被改回归零；
- **测试**：新增 10 例。`PlotBitmapRendererTest` 新增「定义域边界位置精确且与框线留有可见余量」——用记录型 Canvas 读 `drawLine` 的**实际绘制坐标**（Robolectric 的 legacy graphics 不栅格化 Path、`measureText` 每字符恒返回 1px，像素断言都是假绿）。该用例已用「完全还原 v1.0.34 逻辑」的方式反向验证过**确实会失败**（实测边界线落在 901.5 而框线在 933.1，误差 31.6px，与用户截图一致）。`PlotLabelLayoutTest` 新增 `planYTickSize` 的 4 例（装得下沿用基准字号 / 装不下先缩字号 / 缩到下限锁定字号并报告所需宽度 / 空列表与非有限值安全），并重写 x 轴范围用例为「定义域原样保留」「推断范围两侧留白」「单侧定义域只推断另一侧」「模型范围更窄时必须覆盖数据」。

## v1.0.35 (2026-09-15) · 图内符号标注渲染 + 点不开/拖不动的结构性修复 + 最简数值公式

用户反馈：图像本身没问题了，但①坐标轴上的注释没有按公式渲染；②左边的字挡住图像；③**第二张图依然点不开，只有第一张可以，而且「跟第几张无关、靠后的就点不开」，末尾的表格也拖不动**；④单独的 `$0$` 渲染不出来；⑤截图里那条 `\frac B2<|f|<f_c+\frac B2` 整条变成「公式无法渲染」。

- **「靠后的元素点不开」是结构性成因，本轮从两层一起堵**：
  - **Compose 的 `clipToBounds()` 管不住真实 View 的触摸命中**（v1.0.34 已确认）。同一 Column 里文本块是真实 Android 视图，它只要比 Compose 给的格位高一点点，多出的那条带子就盖在下方图片/表格上、把触摸先吃掉 —— 所以表现成「越靠后越点不动」。现在给图片与表格都加 `zIndex(1f)`：Compose 的命中测试按 zIndex 从高到低进行，交互元素**先于文本块**被命中，这一点不再依赖「高度必须绝对精确」；
  - **撤掉 v1.0.34 引入的「高度只增不减」**。它本意是宁可多留空白也不让内容溢出，但宽度变化时换行数会变、高度本该随之变小（流式过程中气泡宽度会从窄变宽），拒绝更新就会让那块**永久占着偏大的高度**，反而成了「靠后元素全被吃掉触摸」的元凶。现在恢复为跟随最新测量值，靠 12 秒观察窗保证观测足够久；
- **坐标轴注释不按公式渲染**：图内标签的判定规则过去只认「反斜杠命令」（`\frac` 之类），于是 `f_c-B/2`、`-B/2`、`f_c+B/2` 这类**纯符号刻度**被判为普通文字原样画出（下划线原样可见）。现在把**下标/上标与数学运算符**也计入判定，交给公式引擎排版。运算符里刻意不含 `-`（连字符在普通文字里太常见），但 `-B/2` 会因 `/` 命中；
- **`$0$` 渲染不出来**：`looksLikeInlineMath` 里有一条「纯数字 → 不是公式」的排除，导致 `$0$` 不会被提升为块级公式、独自留在行内，而同一段里 `$f_c$` 之类都会提升 —— 两条路径不一致，用户看到的就是最简数值公式失效。纯数字在数学语境里就是公式，已去掉这条排除（货币写法 `$5 ... $10` 不会误判：那种 body 必然含空格，仍被后续规则挡掉）；
- **`\frac B2` 导致整条公式失败**：标准 LaTeX 允许 `\frac B2` 这种裸 token 写法，但 **JLatexMath 不接受**，会抛 ParseException 让整条公式退化成灰色占位。新增 `normalizeLatexFractions`，在**图内标签**与**正文公式**两条路径上都把裸参数补成花括号（`\frac{B}{2}`），幂等且不误伤 `\dfrac`；
- **左侧标签挡住图像**：纵轴刻度以前只在画布范围内夹取，参数化的长标签（如 `2π²N_0(f_c+B/2)²`）会横向伸进绘图区压住曲线。现在把夹取范围收紧到**绘图区左侧的留白**，放不下的部分按既有规则截断加省略号；
- **刻度挤成一团**：`-f_c-B/2`、`f_c-B/2` 这类参数刻度比纯数字宽得多，挨着画会糊成一片（截图里 `-f_c-B/2-f_cf_c-B/2`）。现在按「与上一个标签是否重叠」做过滤，宁可少标几个也不叠字；
- **提示词同步**：刻度文案里的数学式要用 `${'$'}...${'$'}` 包成行内公式、`\frac` 必须带花括号、刻度文案控制在 12 字符内；
- **测试**：新增 7 例（`PlotLabelLayoutTest` 4 例：纯符号刻度按公式排版、纯文字不被误判、`\frac` 补花括号、切片路径已规范化；`AssistantTableNormalizeTest` 3 例：数值公式提升、表格单元格内的数值公式、货币写法不误判）。全套单测 **494 例通过**。

## v1.0.34 (2026-09-15) · 修复「图片点不开」+ 坐标轴改用参数符号标注 + 曲线不再贴框被切

用户反馈三件事：① 回答里的前两张图都能点开，**第三张点不开**；② 题目里没有给出数值、横纵坐标含参数时，**应当标参数符号而不是代入的数值**（截图里纵轴被标成了 3900/4200）；③ 有一张图**左右像是被截断、没画完整**，希望像教材那样按参数坐标标注、宽高比例合适、不左右溢出。

- **「第三张图点不开」的真根因：图片上方的文本块实际比 Compose 给它的格位高**。`MarkdownChunk` 装的是真实 Android 视图（interop holder），而 **Compose 的 `clipToBounds()` 只能裁剪 Compose 自己的绘制与命中测试，管不住真实 View 的触摸命中** —— 一旦这个 TextView 的内容比格位高，溢出的那条带子会物理覆盖在下一张生成的图片上，把本该落到图片上的触摸先吃掉。前两张能点、第三张不能，正是「哪个文本块溢出」的偶然结果；
- **修复①：高度上报改为「只增不减」**（单次渲染内）。文本块的高度由公式异步渲染、表格延迟布局等多来源决定，只要有任何一次把偏小的中间值当成最终值上报，就会留下一段永久溢出。现在宁可偶尔多留几像素空白，也绝不让内容超出格位 —— 空白只是观感，溢出会直接吃掉触摸；
- **修复②：高度观察窗从 3 秒延长到 12 秒，并改成「稳定后不退出、只降频到 400ms 心跳」**。原因是 `\begin{bmatrix}` 这类大矩阵公式在 JLatexMath 里单条就要几百毫秒，几个叠在一起可能超过 3 秒；原来的逻辑在高度稳定后就永久退出，于是公式变高发生在退出之后、这个偏小的高度被一路用到底；
- **修复③（诉求②的渲染层根因）：x 轴坐标范围开始留边距**。x 轴用的 `conservativeRange` 此前只取「数据范围 ∪ 模型范围」，**一点余量都不留** ⇒ 曲线首末点必然正好压在绘图区左右边框上，视觉上就像整张图被从中间切断（用户说的「左右看上去还是截断的、没有画出完整的图像」）。现在只在「数据确实贴到该侧边界」时才外扩 4%，模型本来就给宽松窗口时不会被再撑大；
- **修复④（诉求①②的能力侧）：把两条绘图规范写进提示词**。① **题目用符号/参数表达时，坐标刻度也必须用符号**：这类题的 f_c、N_0、B、T 都是参数，题目并没有给出数值，编一个（纵轴标成 3900/4200）等于把图换成另一道题 —— 要求用 `ticks` + `tickLabels` 显式指定刻度位置与文案（`{"-2":"-B/2","0":"O","2":"B/2"}`、`{"1":"N_0(2πf_c)^2"}`）；② **范围要容得下完整图形**：min/max 必须覆盖题目定义域（如理想低通/带通谱只在 |f| ≤ B/2 有定义），并在边界加 `markLines` 竖线，不要让曲线末端压在框线上；
- **配套**：`tickLabels` 的文案长度上限由 16 放宽到 24 —— 16 会把 `$N_0(2\pi f_c)^2$` 这类带 LaTeX 定界符的写法从中间截断，渲染出残缺公式；
- **测试**：新增 `PlotTickLabelParseTest`（2 例）钉住「刻度文案原样保留参数符号与 LaTeX」「没有 tickLabels 时不报错」；`PlotLabelLayoutTest` 的 x 轴范围用例改为验证留白行为（含「模型视野本来宽松时不撑大」与「跨度为零不产生 NaN」）。全套单测 **487 例通过**。

## v1.0.33 (2026-09-15) · 表格渲染修正 + 修复「检查更新能查到、点下载必失败」

同一版里修了两组问题：**表格**（上下拖动与页面滚动冲突、单元格过于空旷），**下载**（地址被叠了两次镜像前缀）。

### 表格：上下拖动与页面滚动打架 + 单元格过大

用户反馈：表格已能左右拖动，但**上下拖动会和整个页面的滚动冲突**；期望**表格高度完整展开全部内容**、不要出现需要上下拖动的固定高度；同时**即使文字很少，单元格也占很大空间**，加剧了内容显示不下。

- **「表格能上下拖动」的真根因：表格行高被算成了 0**。反编译 `TableRowSpan` 后确认，行高公式是 `max(各单元格 Layout 高度) + 2 × padding`，而这个高度**只在 `getSize()` 里被写回**；`getSize()` 内部依赖一张 `layouts` 列表，**该列表只在首次「绘制」时才按可用宽度填充**。于是首次测量时 `layouts` 还是空的 ⇒ `getSize()` 不给 `FontMetricsInt` 赋值 ⇒ **整行被当成 0 高**，表格高度被量成接近 0，我们就把这个偏小的值报给了 Compose。更麻烦的是 **Android 会复用宽度/文本都没变的旧 Layout、不会重算** —— 探针实测「首次绘制之后再 measure」高度依旧是偏小值（111px），**只有显式 `setText(getText())` 强制重排后才变成真实高度（143px，每行 35px）**。高度偏小 ⇒ 内容溢出格位 ⇒ 而 `LinkMovementMethod` 继承 `ScrollingMovementMethod`、`TableAwareMovementMethod` 又包装了它 ⇒ 用户就能在表格里上下拖文字，和页面滚动直接打架；
- **修复①：表格块高度复测 + 强制重排**。表格块（`isTableBlock`）现在和「含公式的内容」一样走高度复测流程；并在复测循环里**按需补发强制重排**——发现高度连续几轮没变就再逼一次 `setText(getText())`，最多 8 次。之所以不用「入口处写死几帧延时」，是因为首帧绘制的确切时刻无法预知，固定延时可能整段跑在绘制之前、白排一场；
- **修复②：表格块彻底不给 `MovementMethod`**（`movementMethod = null`）。表格的横向滚动由外层 Compose 的 `horizontalScroll` 负责、纵向交给页面，单元格内既不需要滚动也不需要选中，留空最干净，也从根上杜绝「表格自己滚」。正文块不受影响，仍有 `LinkMovementMethod`（长按选中/链接照旧）；
- **修复③：单元格内边距 4dp → 2dp**。Markwon 默认来自 `TableTheme.buildWithDefaults` 的 `Dip.toPx(4)`，而行高公式里是 `2 × padding`，density 3 的真机上每行光上下内边距就吃掉 24px —— 文字很少的表格也会显得很空。收紧后每行省下约 12px。注意主题仍从 `TableTheme.create()` 的默认值出发（`asBuilder()`）再改，**不能**改用 `TablePlugin.create { }`：那个入口内部走 `emptyBuilder()`，会把边框宽度/颜色与奇偶行底色一起丢成 0；
- **测试**：新增 `MarkdownTableRenderTest`（4 例）——「表格块必须没有 MovementMethod」「正文块必须保留 MovementMethod」「内边距恰好 2dp 且严格小于默认 4dp」「行高必须强制重排后才算得出来（重排前为 0、重排后 > 0、整表变高）」，其中最后一例专门钉住上面那条根因链，防止后人误删重排逻辑。

### 下载：地址被叠了两次镜像前缀

用户反馈：1.0.23 检查更新后能正常下载，1.0.31 检查更新后**下载失败**，换了镜像站点也不行。

- **根因是「双重镜像前缀」**。清单里的 `apkUrl` **本身就带镜像前缀**（历次 `update.json` 都写成 `https://ghfast.top/https://github.com/...`，云函数只是原样透传）。而 v1.0.31 把下载源顺序改成了「镜像优先」——于是客户端在已是镜像地址的 `apkUrl` 上**又叠了一次前缀**，拼出 `https://ghfast.top/https://ghfast.top/https://github.com/...`。实测这种地址**稳定返回 403**（`ghproxy.net/` 套 `ghfast.top/` 同样 403），所以「检查更新」一切正常、唯独下载必然失败。**1.0.23 之所以没事，是因为它把「原始地址」排在第一位、镜像在后，没有叠加的机会**；
- **修复一（客户端防呆，根因位）**：`DownloadMirrors` 在拼接前先 `stripMirrors()` **剥掉清单里已有的任何已知前缀**（含已失效的 `gh-proxy.com`，老清单可能还在引用），用循环剥离以处理多层嵌套。这样无论清单给的是干净直链还是带前缀地址，**任何一次尝试都不会叠出两层前缀**；
- **修复二（清单侧规范）**：`update.json` 的 `apkUrl` 一律改存**干净的 GitHub 直链**，不再预置镜像前缀——镜像前缀由客户端按「镜像优先 → 直连兜底」自行拼装。本次已把 v1.0.32 与 v1.0.33 的清单都更正，**已装 1.0.31 的用户重新点一次下载即可成功**（无需先升级）；
- **回退链路保持**：镜像（`ghfast.top` → `ghproxy.net`）优先、直连 GitHub 兜底，仍为三次尝试；
- **测试**：`UpdateManifestSourceTest` 扩到 11 例，新增 5 例专钉双重前缀——「清单自带前缀时不叠第二层」「干净地址只叠一层」「已失效前缀也能剥」「多层嵌套一次剥净」「剥离对干净地址与空白幂等」，并用「任一次尝试的前缀层数至多 1 层」做通配断言。

**全套单测 484 例通过。**


## v1.0.32 (2026-09-15) · 图内注释不再溢出图片 + 图表比例修正 + 图片点击链路加固

用户反馈三件事：① 图上的注释文字右侧溢出到图片外；② 依旧「只有回答气泡里的第一张图能点开」；③ 画图的拉伸程度仍然极端（同向正交分量频谱图要突出顶部抛物线，同时体现频谱宽度与高度，但宽高都超出图片）。

- **图表被压扁的真根因：内边距用了固定 dp，而画布尺寸是物理像素**。画布按屏宽 92% 取（典型 993×575 这类固定像素值），内边距却写成 `dp(62f)`/`dp(46f)` 等——dp 会随 density 放大，在 2.75~3.0 的机器上光左右内边距就吃掉 220~240px、上下再吃掉 300+px，**绘图区只剩约 750×250（≈3:1 的扁条）**，曲线被压成一条线，抛物线这类「必须看清形状」的图彻底失真。现在内边距与字号一律**按画布宽高取比例**（左右合计 11%、底部 12.5%，字号约画布宽的 2.4%~3.1%），绘图区宽高比只由画布比例决定、**与 density 彻底解耦**；
- **画布高宽比 0.58 → 0.66**：进一步给纵向留出余量，绘图区稳定落在约 **1.8:1**（此前等效 2.0~3.1:1），既能看清抛物线的弯曲，也放得下横向刻度；
- **图内注释右侧溢出的真根因：夹取只对着「画布」边界，而且装不下时什么都不做**。绘图区里的注释（markArea / markLine 标签）贴着右缘时中心仍按区域中点算，长标签就直接横穿出去。现在① 画图内标签时把夹取边界**临时收紧到绘图区**；② 装不下时**从左往右保留能放下的片段并补省略号**，连一段都放不下就只画省略号；③ 单个超长纯文本片段也走同一套二分截断。**任何情况下都不会有文字画到边界之外**；
- **落位决策抽成纯函数 `planLabelLayout`**：原因是 Robolectric（legacy graphics）里 `Paint.measureText` **每个字符恒返回 1px**（实测 `a`=1、300 个 `a`=300、`汉字测试`=4），像素级「文字是否越界」的断言在测试环境里全是假绿 ⇒ 把几何规则抽出来用受控宽度精确验证（装得下居中、靠右左移、靠左右移、截断保留数与省略号宽度、边界无效不可见等 9 例）；
- **「只有第一张图能点开」的加固**（这个问题的确切触发点受真机排版影响，本轮做的是**结构性封堵**）：`MarkdownChunk` 装的是一个真实 Android 视图（interop holder，**不保证裁剪子视图**），一旦这个 TextView 的实际内容比 Compose 给它的格位高（公式异步加载后变高、或高度上报值短暂偏小），多出来的部分会画到格位之外、压在下一个兄弟节点（通常就是生成的图片）上，而它本身是 clickable + selectable 的真实视图 ⇒ 先把落在图片上的触摸吃掉。现在：① 外层加 `clipToBounds()`，溢出部分既不显示也不参与命中测试；② `setTextIsSelectable(true)` 顺带打开的 `isClickable` 被显式关掉（我们并不需要「点文字」，长按选中/复制不受影响），避免整块文字区域成为一个抢占触摸的落点；
- **提示词同步**：新增「标注要短（超过 12 字会被截断）」「图的比例由客户端决定，min/max 要收在主体特征 + 少量余量」「需要同时体现高度与宽度的图，x 与 y 都要覆盖主体特征本身、不要用 y 范围裁掉峰值」三条约束，从源头减少超长注释与被压平的形状；
- **测试**：`PlotLabelLayoutTest` 扩到 31 例（新增 9 例落位/夹取纯函数回归）。全套单测 **475 例通过**。

## v1.0.31 (2026-09-15) · 检查更新不再依赖云函数（修复「检查更新提示 HTTP 502」）

- **「检查更新」报 HTTP 502 的根因**：清单服务（CloudBase 云函数）跑在免费版，**单次执行上限只有 3 秒**，而它回源 GitHub 要经过 DNS + TCP + TLS + 一次 302 跳转到 CDN 再下载，从上海常态需要 2~10 秒 ⇒ 网络稍慢就必然超时，函数返回 `502 {"ok":false,"error":"暂时无法获取更新信息"}`。实测同一份清单在本机拉取是正常的 200，函数却对**任意 versionCode** 都 502（即与发布内容无关）。
- **修复（App 侧彻底解耦）**：拉取清单改为**多源逐级回退**——① 云函数；② 经加速镜像直取 GitHub Release 附件 `releases/latest/download/update.json`（`ghfast.top` → `ghproxy.net`）；③ 直连 GitHub 兜底。云函数以后再慢/再挂，**检查更新照样能用**（APK 本来就走镜像下载，这条路在手机上已被验证可用）；
- **下载源顺序**改为镜像优先、直连最后（国内手机上直连 GitHub 常不通，排在前面只会白等一次超时）；移除已失效的加速镜像 `gh-proxy.com`（实测返回 403），补入实测可用的 `ghproxy.net`；
- **错误提示不再是技术细节**：界面不再显示 `检查失败：HTTP 502` 这种让人无从下手的文案，改为「检查失败：网络或服务暂时不可用，请稍后重试」；逐源失败原因写进日志便于排查；
- **云函数侧同步改进**（仓库源码，需在控制台重新部署才生效）：回源超时默认值 1200ms → 2000ms（并夹住上限 2500ms，不超 3 秒函数预算），README 记录本次诊断与对策（把源换成可用的加速镜像 + 配置内联清单兜底）；
- **测试**：新增 `UpdateManifestSourceTest`（6 例：云函数优先、镜像优先于直连、每个源都指向清单附件、gh-proxy.com 不得回归、`urlFor` 顺序与越界行为）。全套单测 **467 例通过**。

## v1.0.30 (2026-09-14) · 图内公式标签堆左上角的真根因 + 坐标范围改为「折叠」平衡策略

- **图内文字全堆在左上角——真根因是绘图库不认 bounds 的位置**（v1.0.29 修的「中文叠字」是另一个独立成因）。反编译 `JLatexMathDrawable.draw()` 可见它算平移量时**只用 bounds 的宽高**算居中偏移（`dx=(w-iconW)/2`、`dy=(h-iconH)/2`），**完全没有 `bounds.left/top`** ⇒ 我们 `setBounds(left, top, …)` 对位置毫无作用，**公式一律画在画布原点** ⇒ 标题、图例、带公式的轴标签、标记标签全部叠在图的左上角。修复：**自己 `canvas.translate(位置)`**，再给 drawable 一个原点 bounds（`setBounds(0,0,w,h)`），位置完全由我们掌控；并新增**记录型 Canvas 回归测试**（断言公式标签必须被平移到自己的位置，修复前这里只有 (0,0)）；
- **坐标范围改为「折叠」平衡策略**（上一版「必须覆盖全部数据 + 边距」确实过于极端）：① 用 **Tukey 围栏**（`Q1/Q3 ± 1.5·IQR`）识别离群点，**没有离群点时主体就是全量数据**（不会把正常的极值也裁掉）；② 模型显式给的范围优先，但**至少要覆盖主体**；③ 边距只加在「数据贴着该侧边界」的一边（模型本来就给了宽松窗口时原样保留，不额外撑大视野）；④ 落在范围外的极端点**不撑大坐标轴，而是折叠到边界**——序列绘制裁剪到绘图区，并在竖直轴上画出**断轴标记**（两道短斜线），数据不被无声丢弃；
- **标签定位加固**：公式 drawable 尺寸异常时（0/负宽、或宽过 40 个字号——度量不可信）一律回落纯文本，避免零尺寸 bounds 画不出东西、或超宽标签把整行挤出画布；标签起点被夹在画布内，任何情况下都不会整块跑到画布外；
- **测试**：`PlotLabelLayoutTest` 扩到 23 例（新增 `balancedRange` 六种情形：无离群点留边距、有尖峰时主体仍占视野且标记折叠、模型范围更窄被扩展、模型更宽被保留、无数据兜底、恒定值不产生零跨度；以及公式标签平移回归）。全套单测 **461 例通过**。

> 排查方法备忘：本轮能定位真根因靠的是**反编译库的 `draw()` 看它到底怎么用 bounds**，而不是继续猜。同理，本地"渲染出来看看"走不通——Robolectric 不忠实栅格化（背景画成纯黑、文字无像素），图内文字位置只能真机确认。

## v1.0.29 (2026-09-14) · 修复生成图内文字重叠成团 + 坐标范围过于激进

- **图内文字全挤在左上角、相互重叠——根因是图上的标签被整串丢给了没有中文字形的公式排版器**。JLatexMath 的字体不含 CJK 字形，汉字宽度退化为 ~0：实测 `功率谱` 与 `功率谱功率谱功率谱`（9 字）量出的宽度**完全相同（都是 12px）**，字符只会堆在同一个坐标 ⇒ 一坨叠字。而图上的标题/图例/轴标签是模型自由填的中英混排串（「功率谱密度 $S_c(f)$」），旧逻辑要么取第一个 `$...$` 把中文整段丢掉，要么在「含反斜杠命令」时把**整串（含中文）**当公式交给 JLatexMath —— 后者正是叠加成团的来源。现在标签按「公式 / 普通文字」**分段渲染**：`$...$` 分组交给 JLatexMath；中文与其余文字交给 Canvas 逐段排布（段间 3dp 间距，居中宽度按各段之和计算）；公式体内若含中文也一律退回普通文字；
- **坐标范围过于激进、整体特征被切掉**：模型给了 min/max 时原先**直接采用且不加边距**，而提示词又要求它「范围尽量给全」，于是范围恰好等于数据极值 ⇒ 曲线极值贴着框线（抛物线谷底压在坐标轴底线上、峰顶顶到上边框），形状被切、特征丢失。现在坐标范围**始终覆盖全部数据并保留边距**（y 轴含 8% 边距），模型给的范围只在「比数据更宽」时生效（相当于缩小视野），**绝不用来裁剪数据**；
- **测试**：新增 `PlotLabelLayoutTest`（16 例）——分段规则（纯中文、中文+公式混排、公式体内含中文退回文字、裸命令不含中文仍走公式、**混中文的裸命令必须走文字**、多分组、空串、CJK 判定含全角标点）、坐标范围策略（恰好等于数据/更宽/更窄/未提供 四种情形），以及「**交给公式排版器的片段永不含中文**」这条核心不变量（覆盖 12 种模型真实会写出的标签形态）与「混排标签的中文与公式都要保留」；另新增 `JLatexCjkMetricsTest`（2 例，钉住库级事实：汉字宽度不随字数增长 ⇒ 中文绝不能进公式通道）。
  说明：本轮**没有**做像素级断言——Robolectric（legacy graphics）不真正栅格化文字，位图上量不到文字像素（实测标题带跨度恒为 0，硬编码背景色比对又会长出 899 的假绿），故改钉不变量，画面效果以真机验证为准。全套单测 **454 例通过**。

## v1.0.28 (2026-09-14) · 修复「气泡内文字随滑动位移」与「续写后图表丢失」

- **气泡内文字会随上下滑动位移（先尾部被挡、多划几次变头部被挡）——根因是框高钉在了公式异步加载前量出的偏小值上**。JLatexMathPlugin 的 `placeholder()` 返回 **null**：`setMarkdown` 返回时公式 drawable 还是 0×0，此刻量到的高度缺了所有公式的高度；后台线程算完后插件通过 `Handler` 回主线程 `setResult`，并用 `setText(同文本)` 强制重排，内容真实高度随之变大（已用字节码确认这条链路：`setResult → initBounds → invalidateSelf → 回调 → setText`）。而 Compose 侧的 `Modifier.height` 只在内容/宽度变化时才重测，异步变高它完全感知不到 ⇒ 内容比框高 ⇒ `LinkMovementMethod`（继承自 `ScrollingMovementMethod`）允许**在框内拖动文字**，于是滑动时文字在气泡里上下位移、头尾交替被挡。修复：渲染后**持续复测高度直到稳定**（含 `$` 的内容才轮询，100ms 一次、连续 5 次不变即止、最长约 3 秒），高度一变就同步给 Compose 并复位内部滚动；
- **顺带修掉一个放大该问题的隐患**：`update` 每次重组都会 `setMarkdown` 一遍，而每次重渲染都会把公式 span 重置成待加载、重新排队异步渲染（滚动时反复触发）。现在按「内容+宽度+配色」签名跳过重复渲染；
- **重渲染后复位内部滚动**：内容比框高的瞬间用户可能已把文字拖出偏移，重新渲染时必须清零，否则新内容会带着旧偏移显示；
- **续写后图表整个丢失（正文写着「见下图」却没有图）**：`imagePaths` 原先只用**最后一轮** `reply.plots`。长推导撞上输出上限自动续写时，第 1 轮输出正文 + plots、第 2 轮只续写文字（plots 为空），最后一轮的图列表就成了空 ⇒ 前面轮次已产出的图被整体丢弃，而正文里的「见下图」还在。现在**跨轮累积**各轮 plots；同时把续写轮正文里的 `[[FIGURE:n]]` 锚点按已累积图表数**平移**（模型在续写轮不知道前面已画过几张图，锚点会从 1 重新编号，不平移会指向错误的图）；
- **测试**：新增 `FigureAnchorOffsetTest`（6 例：base=0 原样、单个/多个锚点平移、大小写与留白变体、行内伪锚点不平移、无锚点原样）；`MarkdownChunkInteractionTest` 新增「气泡高度跟随内容变化更新」（41→286，且与独立测量一致，钉住「高度不是渲染一次就钉死」这一契约）。全套单测 **436 例通过**。

> 探针结论备忘：Robolectric 下异步公式的 drawable 尺寸恒为 0（字体度量是桩），公式高度增长**无法**在单测里复现——这是环境失真，别据此以为机制不成立；机制由字节码链路证实，真机验证为准。

## v1.0.27 (2026-09-14) · 修复图片点击与表格手势的最后死角

- **「占位图点了没反应」修复**（用户反馈「只有第一张图可点」的确定性根因）：生成的图表 PNG 被**缓存回收/删除/解码失败**时，`InlineGeneratedImage` 走占位分支——而占位分支原先**没有任何点击处理**，且解码失败结果被 `remember(path)` 永久缓存，从此「点了永远没反应」。现在：① 占位框本身可点击（弹出提示而非静默）；② 解码失败自动重试 3 次（间隔 600ms），覆盖「图刚生成、文件还没落盘」的时序窗口；③ 占位文案区分「加载中 / 已过期 / 无法显示」，用户不再干等；
- **表格彻底放手势**：表格块的 TextView 原先用 `setTextIsSelectable(true)`，它会连带把 View 变成 `clickable+longClickable`——在真机触摸管线上，这类 View 有机会先于父级手势吃掉触摸事件，让横向滚动拖不动（Robolectric 上无法复现、真机嫌疑最大的一条）。表格改为**不可选中**的 TextView，横滑是表格的第一交互，牺牲单元格内长按选中是划算的。真实组件回归测试：横滑满量程（scroll=240）可用；
- **表格溢出加裁剪兜底**：表格是 1.8 倍宽的真实 Android 视图，若不显式裁剪，溢出视口的部分会横向画到气泡外、盖住旁边的文字。滚动容器外包 `clipToBounds()`，画不出去；
- **布局高度收敛钉死回归**：新增 `MarkdownChunkInteractionTest`（Robolectric + Compose UI 测试）——① 文字块高度 == 内容真实高度（41==41）；② 表格块高度 == 宽画布下内容真实高度（111==111，独立测量用与生产同一条归一化管线 + 双次 measure）；③ 表格横滑满量程。v1.0.25 修的「AndroidView 空视图量高」回归若有复发会被这组测试拦下；
- **占位图点击回归测试**：新增 `InlineImageClickTest`，验证占位分支（文件不存在）点击后回调触发。全套单测 **429 例通过**。

> 注：本轮四个反馈症状（图不可点/图重叠/表格拖不动/表格遮字）与 v1.0.24 修复前的描述逐字一致——若升级到 v1.0.27 后仍复现，请截图并注明版本号再报。

## v1.0.26 (2026-09-14) · 修复表格渲染、图片丢失与「回复跑进思考链」

- **表格显示不出来：两个互相独立的成因，都已修掉**。
  ① **表格与上文之间缺空行**——CommonMark 要求表格前必须有空行，否则整块表格被当成普通段落（用真实 Markwon 实测：`TableSpan` 数量为 0，即完全没被识别成表格），渲染出来就是「频率点 ⋯ 值」这样一行带竖线的普通文字。模型在「先说一句、紧接着贴表格」时高频漏掉这个空行，现在 `normalizeAssistantMarkdown` 会自动在表格前后补齐空行（代码围栏内不动）；
  ② **行宽公式拆分把表格行拆断**——`wrapLongFormulas` 是按「整行宽度」决定是否把公式拆成独立块的，一行 `| 频率点 | $$P_{Y_c}$$ |` 里公式一旦量出来超过可用宽度，就会被拆成「`| 频率点 |`」+「`$$`」+「公式」+「`$$`」四行，表格结构当场消失。现在 `wrapLongFormulas` 识别表格行并**整块原样放行**，单元格里的公式交由表格自身的横向滚动容纳；
- **表格单元格里的竖线**：行内公式含绝对值/范数（`$$|f| \le B/2$$`）时，裸 `|` 会被表格插件当成单元格分隔符，导致该行列数与其它行不一致、表格解析错乱甚至整行数据被丢弃。现在公式内（`$$` 区间内）的裸竖线自动转义为 `\|`，单元格分隔符不受影响；
- **生成的图片不显示、正文里裸露 `[[FIGURE:1]]`**：根因是长回答撞上输出上限被截断时，截断抢救路径**只恢复了正文与动作、丢掉了 `plots`**——模型其实已经把图写出来了，客户端却拿不到，`imagePaths` 为空，而正文里的锚点还在，渲染层就把锚点当普通文字显示出来。现在截断抢救会一并恢复**已完整闭合的图表对象**（最后一项不完整的丢掉，前面的照常使用）；
- **锚点不再以字面量露出**：`splitFigureSegments` 现在**无论能否取到图片，都会把锚点行从文字里剥掉**（以前在「没有图」时直接返回原文，锚点就裸在正文里）。同时修掉一个连带问题：无内嵌图时的「正文 + 末尾图」分支原先用的是**带锚点的原始文本**，剥好的片段没有生效；
- **后续回复跑进思考链**：两道修复。① **每轮开始清空思考面板**——原先第 2 轮的思考内容会追加到第 1 轮残留内容之后，面板里堆着两轮拼接的思考；② **把误入思考通道的正文取回来**——部分兼容服务商在续写轮会把答案正文流进 `reasoning_content` 字段，导致正文区停止增长、内容只出现在思考面板里。现在检测到「正文为空而 reasoning 有内容」时，会从 reasoning 里定位最后的 JSON 正文结构（含截断抢救）并把内容接回正文，取不到就保持原样，绝不把真正的思考当正文贴出去；
- **图表可诊断、可自愈**：图表渲染以前**静默吞掉所有异常**，线上出现「图片不显示」时既看不到异常也看不到参数。现在失败原因与图表摘要写入日志，写坏/半截的缓存文件会立即删除，避免下次命中坏缓存；图片缓存被系统回收后，占位文案从一直显示的「图表加载中…」改为「图表已过期」，不再让用户干等；
- **测试**：新增 `AssistantTableNormalizeTest`（6 例：补空行、保留原有空行、公式内竖线转义、代码围栏不动、伪表格不动）、`TableFormulaWrappingTest`（4 例：表格行绝不被拆、正文超宽公式仍拆）、`SalvagePlotRecoveryTest`（3 例：截断恢复 plots、坏项丢弃不影响其余、正常 JSON 不受影响）、`SalvageReplyFromReasoningTest`（5 例：完整/截断 JSON 取回正文、纯思考不臆测、结果不等于输入），并更新 `FigureAnchorSplitTest` 覆盖锚点剥离。全套单测 **425 例通过**。

## v1.0.25 (2026-09-14) · 修复「只有第一张图能点开」+ 图内公式可渲染

- **修复「回答里只有第一张图片能点开，后面的图和表格都点不动」**（根因是高度上报缺失）：`MarkdownChunk` 用 `AndroidView` 承载 Markwon 渲染的 `TextView`，而 **Compose 在测量阶段拿到的是一个刚创建、还空着的** TextView（内容要等 `update`/`LaunchedEffect` 才写入），量出来只有一行高度。等文字、公式、表格真正填进去，实际高度早已超出这个数字，但父 `Column` 仍按「一行高」给后续兄弟节点排布 ⇒ 后面的图与表格被压进前面那块撑开的区域，触摸命中随之错乱。现在渲染完会**按精确宽度主动重测并上报真实高度**（`measuredHeightCompat`），布局与命中都恢复正常；
- **表格横向拖动的第二个隐性问题**：外层 `Box` 原先用 `fillMaxWidth`，而 `horizontalScroll` 只在「内容宽 > 容器宽」时才产生可滚动区间——被撑满后滚动距离为 0，横滑毫无反应。改用 `wrapContentWidth`，可滚动区间才真正存在；
- **给图文交替的渲染片段加显式 `key`**：`Column` 内容没有 key 时按**位置**差量复用，上一帧 `[文字块, 表格块]` 变成这一帧 `[文字块, 图, 表格块]` 时，槽位会被原地复用，而里面挂的是真实 Android 视图、复用时机远比纯 Compose 节点脆弱，容易留下尺寸与位置都错配的旧视图。显式 key 让 Compose 按身份匹配；
- **图内公式现在真的能渲染了**（原先被判定为「不可行」，这次实现了）：绘图用的 Canvas 本身没有公式排版能力，但 `JLatexMathDrawable` 就是一个普通 `Drawable`（有 `draw(Canvas)`、`setBounds`、`intrinsicWidth/Height`），完全可以画进图表位图。新增 `drawSmartText`：**自动识别**标签里的 LaTeX（`$...$` 优先取中间内容；没有美元符号但含 `\frac`、`\Delta` 这类命令则整串当公式）→ 交给 JLatexMath 排版；识别不出或写法非法时**安全回落到纯文本**，绝不让整张图失败。标题、轴标签、图例、`markLine`/`markArea` 标注全部走这条路径，公式结果按字号缓存避免重复解析；
- **测试**：新增 `PlotLatexLabelTest`（7 例，覆盖标题/轴标签/裸命令/markArea/全 LaTeX/非法回落/纯文本不变）+ `MarkdownChunkHeightTest`（4 例，用真实 `TextView` 验证「空视图量出的高度会一直被沿用」这一根因）。全套单测 **406 例通过**。

## v1.0.24 (2026-09-14) · 修复 v1.0.23 引入的三个交互回归

- **大图预览恢复双指缩放**：v1.0.23 为了修「左右滑动翻页」把手指路径拆成了两条——未放大只挂 `detectVerticalDragGestures`、放大后才挂 `detectTransformGestures`，结果人为造出一个**缩放死区**：未放大时没有任何人处理 `zoom`，双指捏合完全失效。现在改成**自己写手势循环**：双指捏合、放大后单指平移都消费事件；单指未放大且以横向为主时**主动不消费**，把事件让给 `HorizontalPager` 翻页；单指未放大以纵向为主时做下拉关闭。缩放与翻页同时可用；
- **生成的图表真正插进正文中间**：v1.0.23 把图统一追加在 `MarkdownAnswer` 之后的 Column 末尾，于是「正文说见图 1」和图之间隔着好几段文字。现在按提示词约定的独立行锚点 `[[FIGURE:n]]`（1-based 对应 `plots` 下标）把正文切成「文字 / 图 / 文字 / 图」交替渲染，图出现在讲解它的那段话旁边；模型没写锚点时退回「正文 + 末尾图」旧行为，**不会丢图**；越界锚点（写了 `[[FIGURE:3]]` 但只有 2 张图）直接忽略、也不吞正文；
- **表格能拖动了**：外层 `SelectionContainer` 会安装自己的 `pointerInput` 并把事件标记为已消费，内层表格的 `horizontalScroll` 因此收不到横向拖动。现在把 `SelectionContainer` 从气泡外层**收窄到只包住文字**，图片与滚动容器移到它外面；
- **图片能点开看细节了**：同一个 `SelectionContainer` 也吞掉了图片 `clickable` 的点击事件（与上一条同源）。移出后用户拍的缩略图和生成的图表都能正常点开大图；
- **回归测试**：新增 `FigureAnchorSplitTest`（10 例）覆盖锚点切分——多图顺序、行内留白/大小写/空格变体、越界丢弃、行内伪锚点、文首文末空段等；全套单测 395 例通过。

## v1.0.23 (2026-09-14) · 图片查看与图表显示改进

- **大图预览的左右滑动修好了**：原来无论是否放大都用 `detectTransformGestures`，它会把横向拖动一并消费掉，`HorizontalPager` 根本收不到事件，翻页完全失效。现在按缩放状态分流——**未放大时只接纵向拖动**（下滑关闭），横向交给 Pager；**放大后**才接管全部手势用于拖动看图；
- **生成的图表内嵌整行显示**：助手生成的图不再和用户拍的照片一样挤成 92dp 缩略图，而是**独占一整行、宽度撑满气泡**，排在回答正文之后；点开仍是全屏大图（可缩放、可左右切换）。用户自己拍的照片保持原缩略图形态不变；
- **修掉图内元素重叠**：图例原先画在绘图区内部左上角，会被曲线压住（左右对称的谱线尤其明显），现在移到标题下方、绘图区之外横向排布；`markLine` 标签也移到绘图区顶部，与区间标注错开高度，不再叠字；
- **图内文字支持 LaTeX 降级**：模型常把 `$S_c(f)$`、`\frac{a}{b}` 直接写进标题和轴标签，而画布没有公式排版能力，于是图上冒出一串美元符号和反斜杠。现在会剥掉定界符、把常见命令映射成 Unicode（`\pi`→π、`\le`→≤）、上标转成 ² / ³、分式退化成 `a/b`；提示词也要求模型图内文字直接用 Unicode。

## v1.0.22 (2026-09-13) · AI 助手会画图了

- **新增绘图能力**：用户说「画一下 y=x² 与 y=2x² 的对比」或题目要求「求功率谱并画出图形」时，助手会输出**结构化的绘图参数**，客户端**本地原生渲染**成图、挂在回答下方，点开可放大、捏合缩放——直接复用现有图片查看器，**UI 零新增**；
- **技术路径**：模型只回答「画什么」（坐标系 + 曲线表达式或数据点 + 标注），**不生成图片、不写代码**；客户端走 `PlotSpec → 采样 → Canvas 绘制 → PNG 缓存`；
- **安全**：表达式交给**白名单求值器**（变量只允许 `x`，函数限 sin/cos/tan/exp/ln/sqrt/abs 等，常量 pi/e），**绝不执行模型给的字符串**；写坏的表达式在解析阶段就被拦下，只丢这一张图并提示，不影响正文；
- **覆盖面**：函数曲线、频谱/功率谱、波形、实测散点、眼图（多曲线叠加）——全部共用同一套「坐标系 + 点集」渲染器（表达式先采样成点，与显式点集走同一条路径）；
- **细节**：自动刻度（1/2/5 序列）、自动范围（留边距，所以 ±4% 的上翘也看得出来）、**发散点断线**（`1/x` 在 0 处不会连出一条竖线）、虚线/区间标注、图例、面积填充；
- **实现方式**：核心层（模型 / 刻度 / 求值 / 采样）是**纯 Kotlin、无 Android 依赖**，可直接单测；先在 PC 上用 Java2D 把同一套绘制流程验证通过，再移植到 Android（只替换绘图 API）。

## v1.0.21 (2026-09-13) · 宽表格不再被挤成一团

- **问题**：表格列数多、单元格里又有公式或长句时，会被压缩到气泡宽度——公式缩成小字号、中文竖排，基本读不了；
- **渲染侧：表格单独横向滚动**。含表格的回答会按「表格块 / 正文块」切开渲染，表格按气泡宽度 **1.8 倍**的画布绘制（先给列宽更多空间），仍放不下时可以左右滑动；正文块不受影响、照常铺满宽度。**不含表格的回答走原路径，行为完全不变**；
- **模型侧：提示词加了表格约束**。要求单元格只放短标签、数字或单个符号，公式一律写在表格外、格子里用「(1)(2)」编号对应；列数控制在 4 列以内，更多就拆成两张表或改用列表——从源头减少"表格里塞公式"这类天生不适合的写法；
- 新增 5 例切分单测（表格+正文混合、纯正文、代码围栏内的竖线、缺少分隔行的伪表格、带冒号对齐的表）。

## v1.0.20 (2026-09-13) · 修掉上一版引入的换行误判

- **现象**：正文里冒出字面的 `\nf(x)`、`\nuv`，该换行的地方没换行、文字连成一片；
- **根因（上一版引入）**：为修复 LaTeX 反斜杠撑破 JSON，加了「反斜杠 + b/f/n/r/t + **后面还跟字母** = LaTeX」的判据。这条对 `\frac`、`\text`、`\beta` 都好用，**唯独对 `\n` 是错的**——正文换行后紧跟英文单词（`f(x)`、`uv`、`network`）时，那个 `\n` 本来就是真正的换行，却被误还原成字面反斜杠 n；
- **修法**：`\n` 单独判断——只有后面拼出来的词是已知的 n 开头 LaTeX 命令（`\nabla`、`\neq`、`\notin`、`\nu` …，**精确匹配**）才按公式处理，其余一律按真正的换行；`\t`、`\f`、`\b`、`\r` 维持原判据（它们的字面义制表/换页/退格/回车几乎不会出现在正文，而 `\text`、`\frac`、`\beta`、`\rightarrow` 是高频命令）；
- 新增 2 例回归：换行后跟英文不被误判、`\nabla` / `\neq` / `\notin` 仍被保留。

## v1.0.19 (2026-09-13) · 揪出「怎么改都渲染不出来」的那类公式

- **根因：`equation` 环境没人转换**。清洗管线用一张白名单（aligned / align / gather / equation / cases / matrix …）决定给哪些数学环境补 `$$` 包裹，但真正做环境改写的函数**只处理 `aligned`**，其余原样放行。而实测证明 **JLatexMath 恰好不认识 `equation`**（报 `Unknown environment: equation`），于是 `\begin{equation}…\end{equation}` 被补上 `$$` 后直接送进渲染器，**每次都抛异常、每次都落到占位符**——这就是「同一处公式改多少遍都渲染不出来」的原因；
- **修法**：equation 只表示「独立成行 + 编号」，剥掉标签对公式内容毫无影响，现在会剥壳后再由外层补 `$$`；
- **顺带降级一批需要额外宏包的命令**（逐个实测确认 JLatexMath 里不存在）：`\cancel{x}` → `x`、`\color{…}` → 剔除、`\intertext{…}` → 换行 + `\text{}`、`\begin{dcases}` → `cases`；
- **澄清两个曾被怀疑的「结构性缺陷」，实测都不成立**：JLatexMath 对中文（`\text{持续}`、`\mbox{中文}`）和直接输入的 Unicode 数学符号（`≤ α → ∑ × ≠ √ ∫`）都能正常渲染，无需特殊处理；
- 新增 `LatexCapabilityProbeTest`：105 个常见 LaTeX 构造逐个喂给真实解析器、结果写进 `build/latex-probe.txt`。以后再遇到「某个公式渲染不出来」，跑一次就能分清是**库本身不支持**还是**清洗没覆盖**。

## v1.0.18 (2026-09-13) · 公式把 JSON 协议撑破，导致回答变「乱码」

- **现象**：部分回答会把模型的协议原文直接显示出来（正文开头是 `{"reply": "…`，结尾拖着一串 `plan_actions: [], "english_actions": []`）；有时公式又变成乱码样的字符；
- **根因**：模型在 reply 正文里写 LaTeX 时经常直接输出**单反斜杠**（`\frac`、`\alpha`、`\cdot`），而 JSON 只允许 `\" \\ \/ \b \f \n \r \t \uXXXX` 这几种转义——
  - `\a`、`\c` 这类**非法序列让整包解析失败**，解析器全线放弃后只能把原文当回答显示；
  - `\f`、`\n`、`\t` 虽然合法，却会把 `\frac` 悄悄吃成「换页符 + rac」，公式被改坏但不报错；
- **修复**：解析前统一修复转义——LaTeX 命令都是「反斜杠 + 多个字母」而 JSON 转义只跟单个字符，据此把 `\frac`、`\alpha` 还原成真反斜杠，同时保留真正用于换行的 `\n`（后面跟中文/标点的不受影响）。三条解析路径（整包、候选子串、截断抢救）都走这套修复；
- **同时修「提前戒断」**：原先单轮产出不足 200 字符就判定「模型在原地打转」并停止续写，但**思考占满额度、答案刚开头就被截断**也会命中，于是长回答会在中途停住（把续写上限调高到 8 段后更明显）。现在改为**连续两轮**都挤不出内容才停；续写指令也写明了「不要提前收尾或总结」。

## v1.0.17 (2026-09-12) · 追问时不再丢失之前拍的照片

- **问题**：多模态模式下拍照提问，**追问时那张照片会被丢掉**——接口是无状态的，图片只在拍下那一轮发过一次，而代码里只有"最后一条用户消息"才带图（历史消息的图片路径只是本机路径，从不外发）。数学题还能靠上一轮回答里转述的题目凑合，**英语阅读这种长文章就直接整篇丢失**，于是追问开始编；
- **修复**：追问时按策略**回溯携带历史图片**——自动带上最近 **2 条带图消息**的图片（合计 base64 上限约 8M 字符，约 6MB 原图），读不到（照片已清理）或超预算就跳过，绝不会因此让整轮提问失败；
- **仅多模态直发模式受影响**：纯文本模型的本地 OCR 会把文字直接写进消息正文，历史里一直在，不受此问题影响；
- 图片只在请求里回溯携带，不落库、不进备份，存储占用不变。

## v1.0.16 (2026-09-12) · 修复长回答续写后的上下文丢失与幻觉

- **根因：历史窗口被续写挤爆**。取上下文用的是「最近 10 条消息」，而自动续写每轮都会往对话里追加两条（回答片段 + 内部指令），窗口一滑就把**最初的那个问题**挤出视野——模型看不到自己在回答什么，只能顺着「继续」往下编。现在改为**以本轮开始时的历史为锚**，续写时显式回传「原对话 + 已生成的内容 + 继续指令」，不再依赖滑动窗口；
- **追问丢上文**：注释写的是「最近 10 轮」，实际只取了 10 条消息（5 个来回），多问几轮就丢。现在按**条数（24）与总字符（24000）双限**取历史，只有真的超长才从最早的开始丢；
- **续写指令不再污染会话**：内部用的「继续，从刚才中断的地方接着输出」原先会被永久写进会话，聊久了历史里积一堆「继续」，既干扰模型也让回看混乱；现在只用于当次请求；
- **长回答不再裂成多个气泡**：续写的多段回答现在写进同一条消息（原来一段一个气泡，回看是碎的，喂给模型的历史也是碎的）；
- **续写不再重复联网**：首轮已经搜过，续写轮再搜一次既慢，又可能引入与上文冲突的材料。

## v1.0.15 (2026-09-12) · 查清 DeepSeek 搜不到的根因

- **结论：不是请求写错了，是服务端不再执行搜索**。8 月初社区实测 DeepSeek 的 `web_search` 可用，当时跑的是 V4-Flash；9 月 10 日 DeepSeek 上线 V4.1 Flash 并下线 V4 Flash（旧模型名 `deepseek-v4-flash` 会被路由到 V4.1 Flash）。V4.1 Flash 的 Responses 兼容层**忽略**内置工具（web search / file search / code interpreter / computer use / MCP），请求返回 200 但不会真的搜索——官方 API Reference 仍写着"支持、服务端执行"，与实测和官方兼容性明细表相矛盾；
- **自检新增「最小请求」对照**：除了当前请求，会再发一条只有 `model` / `input` / `tools` 的官方最小示例请求。若连它都搜不到，就可判定是服务端问题，而不是本地附加字段（`instructions`、`max_output_tokens`、`tool_choice`）干扰；
- **自检结果现在显示实际使用的模型名**，便于核对是不是踩到了已下线的旧模型名；
- 联网仍然可用的是小米 MiMo（Chat Completions），DeepSeek 侧若要联网需等官方恢复或改用第三方搜索。

## v1.0.14 (2026-09-12) · 按服务商选对联网协议 + 自检挪进模型编辑

- **MiMo 自动走对协议**：它的 Responses 网关明确拒绝 web_search（HTTP 400 `responses_feature_not_supported`），而 Chat Completions 实测可用（一次检索拿到 25 条来源）。现在只要端点是小米 MiMo，即使协议选了 Responses 也会自动改走 Chat Completions，不再白跑一次失败请求；
- **其它服务不再被 400 卡住**：DeepSeek 的 Chat Completions 只接受 `function` 类型的 tools，塞 `web_search` 会直接报 `unknown variant`。现在按服务商分开写联网参数——MiMo 用 tools 声明，其它服务改用顶层联网开关字段；
- **自检挪进模型编辑对话框**：设置页 AI 区那个会被挤成竖排的「检测联网搜索」按钮已移除，改到**每个模型自己的编辑界面**里，紧贴「联网搜索协议」选项——改完协议就能立刻测。测试使用编辑器里正在编辑的地址/模型/协议，还没保存也能先测；
- **如实告知 DeepSeek 的现状**：它的官方 API 目前未开放服务端联网搜索（Responses 接受请求但不执行、Chat Completions 拒绝该工具），检测结果会直接说明，需要联网时优先用 MiMo。

## v1.0.13 (2026-09-12) · 联网搜索：不再静默失败

- **联网搜索默认开启**：之前设置页的总开关默认关闭，而对话页的「智能搜索」开关绕不过它，于是经常出现"明明开了却没反应"。现在默认开启，并且打开对话页开关时会顺带把总开关打开；
- **联网失败不再悄悄降级**：接口报错、模型不支持、端点拼错，回复里都会写明「联网搜索未生效，已改用离线回答（原因）」，不再静默退回普通对话；
- **能识别"请求被接受但压根没搜"**：部分服务商会忽略 `web_search` 工具——请求返回 200、回答正常、但一次都没搜过。这种情况现在会明确提示本次实际是离线回答；
- **新增「检测联网搜索」**：设置页点一下，同时试 Responses 与 Chat Completions 两种协议，报告 HTTP 状态、耗时、是否真的发起搜索、拿到几条来源，直接告诉你要把协议改成哪个；
- **强制联网更可靠**：打开「智能搜索」时请求会带上 `tool_choice`，让模型必须执行搜索，而不是全靠它自觉；
- **来源识别更全**：除 DeepSeek 的 `citations` 外，也兼容 OpenAI 风格的 `annotations` 引用。

## v1.0.12 (2026-09-10) · 模型列表随时取 + 城市自动定位

- **「获取模型列表」不再要求现输密钥**：编辑已有模型配置时密钥框是空的（密钥加密保存、不回显），之前按钮因此永远灰着、点不动。现在只要该配置存过密钥，随时可点、也可重复点，按钮下方会说明「将使用该配置已保存的密钥获取」；
- **模型名已填也能拿到完整列表**：之前点「选择」会拿输入框里的文字去过滤列表，看起来像「只返回了那个模型名」；现在始终展示接口返回的完整列表，当前选中的那一项加粗；
- **获取失败不再清空已有列表**：上一次成功拿到的列表会保留，仍可手动挑选；连续点击时只有最后一次请求的结果生效，不会被慢请求覆盖；
- **城市改为自动定位**：设置 → AI 学习助手 里的「所在城市」不再手输，点一下按粗略定位（Wi-Fi/基站级）自动填城市名，定位中与失败都有提示，不想用可随时清除；定位只取大致城市，不上传任何位置数据；
- **称呼输入框占位简化**：去掉「小李/老板」举例，改为「请输入昵称」。

## v1.0.11 (2026-09-09) · 交互三连修 + AI 记住你的称呼

- **图片查看器支持缩放**：AI 助手里点开照片后，双指捏合缩放（1~5 倍）、双击放大/还原、放大后单指拖动查看细节，越界自动回弹；未放大时点空白或下滑关闭不变；
- **新建对话图标不再像删除**：对话中右上角图标从垃圾桶换成「气泡+号」，行为本就是开启新对话；历史列表里的删除（垃圾桶）不受影响；
- **时段「至少完成几项」输入修复**：点击输入框自动全选、直接输入即覆盖，可清空；范围 0~20，空值/越界有提示，失焦自动还原；
- **AI 可以帮你改「每时段至少完成几项」**：对助手说「把上午时段改成至少完成 3 项」即可生成待确认方案，确认后计划页同步；
- **AI 记住你的称呼与城市**：设置 → AI 学习助手新增「AI 对你的称呼」「所在城市」，保存后所有回复用你的称呼，且时间相关回答（今天/本周/昼夜问候）以注入的当前时间与你所在城市为准；留空则不注入，不影响默认行为。

## v1.0.10 (2026-09-08) · 长回答自动续写（自适应，无固定轮数上限）

- **长回答被截断修复**：公式推导很费 token，输出撞到 max_tokens（finish_reason=length）时，半截 JSON 被抢救后原样展示，结尾就是「**（2」这种断句。现在检测到截断后**自动发「继续」接着生成**——没有固定轮数上限，只要每轮仍在产出实打实的长内容就一直续下去；文本/图片两条发送路径共用同一套逻辑；
- 三道刹车防失控（正常推导几段内自然写完，碰不到）：① 设置页「AI 学习助手 → 长回答自动续写」保护上限滑杆（默认 4 段，0 = 关闭自动续写）；② 单段几乎无新内容（< 200 字符）视为模型已收尾/打转，停止；③ 累计输出总长度保险丝；
- 仍被截断收尾时：去掉悬空的加粗标记、补齐未闭合的公式块，并明示「发送继续可接着生成」；各刹车触发时均给出原因提示。

## v1.0.9 (2026-09-08) · 用户气泡靠右修复

- **用户消息气泡跑到左侧（与 v1.0.8 输入框调整无关的独立回归）**：v1.0.5 改气泡宽度时在 `BoxWithConstraints` 内多包了一层收缩宽度的 `Box`，`align(CenterEnd)` 变成对"气泡自身宽度"对齐——等于没对齐，用户气泡全部靠左、和助手气泡同侧。去掉多余包裹，`align` 直接作用于全宽作用域：用户气泡靠右（橙色小气泡在右侧），助手气泡靠左，位置区分恢复；气泡宽度自适应逻辑不变；
- 输入行 ⊕ 按钮放回输入框与发送之间（v1.0.8 误移到左侧工具组，已还原）。

## v1.0.8 (2026-09-08) · 公式 NPE 根治（R8 keep）+ 拍照裁剪闪退修复 + 输入框位置

- **公式「⚠ 公式无法渲染 (NullPointerException)」根治**：jlatexmath 内部用 `Class.forName(类名).newInstance()` 反射实例化 XML 预定义命令的实现类（`\operatorname`、`\dfrac`、`cases` 环境等一批），R8 把类改名后按原名查找失败 → 返回 null → NPE。真机表现恰好是：内容完全合法却失败、只有走反射注册的那部分公式挂（硬编码路径的 `\int`/`\frac`/`E[…]` 正常）、debug 不混淆所以测试全绿。修复：proguard 整包保留 `org.scilab.forge.jlatexmath.**` 原名（官方推荐做法，体积代价约 1~2MB）。这个问题从 v1.0.1 开启混淆起就存在，是历次「时好时坏」占位框的总根源之一；
- **第一次拍照有概率闪退裁剪界面（第二次正常）**：待裁剪队列 `cropQueue` 用普通 `remember` 存，部分 ROM（MIUI/鸿蒙）对 `requestedOrientation` 的每次写入都可能触发界面重建，队列一丢刚弹出的裁剪界面就消失；第二次拍照时方向没有变化、没有写入动作，所以正常。修复：队列/打卡照片列表改 `rememberSaveable` 存路径（重建不丢）；方向守护在「当前方向本来就没被相机带偏」时跳过强制转向写入，只交还系统；
- **输入框移到发送键一侧（页面右端）**：⊕（附带/快捷）收进左侧工具组，布局变为 相机 → 相册 → ⊕ → 输入框(带🎤) → 发送；
- **公式失败占位可诊断**：占位框标题带异常类型（如 `⚠ 公式无法渲染 (ParseException)`），失败 latex 与异常写入本地 `assistant-render.log`——本版 NPE 正是靠用户截图里的异常类型锁定的。

## v1.0.7 (2026-09-07) · 裁剪弹窗横屏修复 + 公式裸环境修复

- 拍完照的裁剪弹窗在（平板）横屏下上下被裁、看不到比例条和保存按钮：根因是视口约束写成 `fillMaxWidth().widthIn(max)` —— **fillMaxWidth 先把宽度钉死成父容器宽度，后面的 max 形同虚设**，正方形按整个屏宽算高（1280dp+），整个弹窗比屏幕还高。改为 `sizeIn(maxWidth, maxHeight)` 在前 + `fillMaxWidth()` 在后，并把视口边长改为「窗口高度 − 260dp（标题/提示/比例条/按钮的预算）」、下限 200dp；
- 弹窗内容整体加 `verticalScroll` 兜底：极小屏/分屏下内容超高时可滚动，裁剪框手势不受影响（框内 pointerInput 自行消费拖动）；
- **数学公式以源码露出修复（平板截图坏例）**：模型最常把分段函数写成不带 `$$` 的单行 `\begin{cases}...\end{cases}`，环境补包裹逻辑只往后续行找闭合、单行永远匹配不到 → 整段落成 Markdown 纯文本（`\\` 被吃成 `\`、`&` 原样露出）。现在同行闭合的环境也自动补 `$$`（行内已带 `$$` 的不动）；顺带修复 `$$y(n) =`（`$$` 后带内容）开不了显示块、以及该场景下补包裹会产生两对 `$$` 错配的问题；新增 6 例回归测试。

## v1.0.6 (2026-09-07) · 方向恢复加强 + 气泡再放宽

- **拍完照仍卡在竖屏（v1.0.5 未解决）**：v1.0.5 的做法是「启动相机前 LOCKED，回来解除锁定交还系统」——实测在鸿蒙上无效，因为**解除锁定后系统会按它认为的当前方向继续显示，而那往往就是相机留下的竖屏**。改为记住拍照前的方向，回到前台时**显式强制**转回（`SENSOR_LANDSCAPE` / `SENSOR_PORTRAIT`），转屏动画结束后（1.5s）再交还 `FULL_USER`，不把方向写死；
- **气泡仍偏窄**：最大宽度从 `min(可用宽 × 0.86, 760dp)` 放宽到 `min(可用宽 × 0.92, 1400dp)`。760dp 在 1200dp+ 的平板横屏上只占六成，看着仍是"窄一条靠左"；现在基本铺满可用宽度，只留一点对侧留白区分收发双方（手机端手感不变）。

## v1.0.5 (2026-09-07) · 屏幕方向 / 平板横屏适配

三处与方向、横屏相关的 bug 修复：

### 1. 拍照被强制切竖屏、拍完不恢复（打卡拍照 / 饮食拍照 / AI 拍题）

- **根因**：`MainActivity` 未声明 `screenOrientation`，系统会按 `unspecified` 处理，平板等大屏设备上容易被套用「方向/信箱兼容模式」；同时系统相机普遍在 manifest 里声明竖屏，启动后会把屏幕带成竖屏，且**不受用户的方向锁定约束**——返回后部分 ROM（鸿蒙/EMUI、MIUI）不会把调用方 Activity 纠正回来，于是卡在竖屏；
- **修复**：
  - `AndroidManifest.xml`：`MainActivity` 增加 `android:screenOrientation="fullUser"`（明确支持四向，尊重用户锁定）与 `android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|fontScale|fontWeightAdjustment"`（旋转不再销毁重建 Activity）；
  - 新增 `ui/util/ScreenOrientationGuard`：启动相机前把方向锁在当前方向（`SCREEN_ORIENTATION_LOCKED`），`MainActivity.onResume` 回到前台时恢复 `FULL_USER`——系统按「顶层 Activity 的方向要求优先」裁决，相机退出后会自动转回我们锁定的方向；
  - 三处拍照入口（`CheckInDialog`、`MealScreen`、`AssistantScreen`）在 `takePicture.launch()` 前调用 `armBeforeExternalCapture()`；
  - 待回写的临时文件路径从 `remember` 改为 `rememberSaveable`（存路径字符串），旋转或进程被回收后重建也不会丢照片。

### 2. AI 助手旋转后布局错乱，必须重新进入才恢复

- **根因**：旋转会销毁重建 Activity（未声明 `configChanges`），重建期间「正在等相机返回」的临时文件与滚动位置丢失；同时回答正文是 `AndroidView` 里的 Markwon `TextView`，公式按「首次拿到的宽度」拆分，宽度变化后不会重新排版，于是沿用旧宽度拆出的超长公式，表现为内容溢出/错位，只有重新进入（重新创建 composable）才恢复；
- **修复**：
  - 上述 `configChanges` 让旋转只触发重组、不重建 Activity；
  - `MarkdownAnswer` 改为「测到真实宽度才渲染」：用 `Modifier.onSizeChanged` 记录 `TextView` 实测宽度，宽度/内容/配色变化时重新跑一次渲染（`LaunchedEffect` + 抽出的 `renderMarkdown()`），彻底去掉「用屏幕宽度兜底」的旧逻辑；
  - 裁剪弹窗 `PhotoCropDialog`：视口尺寸变化（旋转/分屏）时把裁剪框按比例迁移到新坐标系（`scaleRect`），不再是停留在旧位置的错位选框；预览图高度由写死 420dp 改为 `min(420dp, 屏高 × 55%)`，横屏下按钮不再被挤出屏幕；裁剪视口正方形边长按 `min(屏宽, 屏高 × 56%)` 封顶。

### 3. 对话气泡宽度不随平板变宽 + 公式溢出

- **根因**：气泡最大宽度写死 `widthIn(max = 480.dp)`，平板横屏可用宽度 900dp+ 时仍只有 480dp；公式可用宽度 `formulaMaxWidthPx()` 在 `view.width == 0`（首次渲染尚未布局）时用**屏幕宽度**兜底，平板上远大于气泡宽度，等于「不拆公式」→ 超出气泡被裁；
- **修复**：
  - 气泡最大宽度改为 `min(可用宽度 × 0.86, 760dp)`（用 `BoxWithConstraints` 取父容器实际可用宽度，比分屏下的屏幕宽度更准）：手机 400dp → 344dp 手感不变，平板横屏 1200dp → 760dp；
  - 公式宽度只认 `TextView` 实测宽度（减去 24dp 留白），拿不到宽度就先不渲染（最多晚一帧），不会再按屏幕宽度错误拆分。

## v1.0.4 (2026-09-07)

- 更新安装完成后自动清理旧安装包（只保留最新一个），释放存储空间；
- 百度网盘下载备份包显示实时进度条（百分比 + 已下载/总大小），下载慢时不再是「无响应」的空白等待。

## v1.0.3 (2026-09-07)

- 下载器修复：APK 保存目录与查找目录统一为 `files/Download/apk/`，并用 `COLUMN_LOCAL_URI` 精确定位下载结果 + 双目录兜底扫描，修复鸿蒙平板「下载完成后未找到 APK 文件」；
- 更新弹窗修复：版本号/大小/更新日志不再显示成 `${...}` 原始占位符；
- 下载失败可感知：DownloadManager 失败不广播，改为 5 秒轮询，失败后自动切换加速镜像重试（最多 3 源）。

## v1.0.2 (2026-09-07) · 英语背诵复习（SM-2 间隔重复）

- **背诵页**：墨墨式翻转自评卡片——正面只显示英文，默想释义后点卡片翻面，再选「认识 / 模糊 / 忘记」；顶部显示第几张与进度条，结束页汇总本轮三档数量；
- **SM-2 间隔重复**：新增 `Sm2Scheduler` 纯函数（`domain/english/`），间隔按 1 → 6 → 上次间隔 × 难度系数 递增；「模糊」间隔 ×1.2 并降低难度系数，「忘记」归零且 10 分钟后重来；难度系数下限 1.3；配套 8 个 JVM 单测；
- **每日新学上限**：默认 20 条/天，设置页「英语背诵」可滑动调整（5～100）；到期复习不限量；
- **数据层**：`english_entry` 增加 `review_due_at / review_ease / review_interval_days / review_reps / review_lapses / review_last_at`，数据库 v10 → v11 非破坏迁移（老条目自动成为新卡）；新字段自动纳入多端同步与版本化备份；
- **入口**：英语积累本顶部新增「今日背诵」卡片，显示待复习 / 可新学数量，一键进入背诵页（路由 `english/review`）。

## 2026-09-06 ～ 2026-09-07 · 应用内自动更新 + release 瘦身 + 开源

- **release 瘦身**：单 arm64-v8a + R8/资源压缩，APK 410MB → 284MB（OCR 模型 212MB 原样保留）；x86_64 仅 debug 构建保留（模拟器用）；
- **应用内自动更新全链路**：启动静默检查（仅 Wi-Fi / 每天一次 / 总开关三重节流）→ 发现新版本弹窗（版本号/大小/更新日志）→ 系统 DownloadManager 下载 → SHA-256 校验 → 提示覆盖安装；设置页「更新」卡片支持手动检查，无新版本显示「已是最新版本」，失败显示原因；
- **包名统一**：applicationIdSuffix 移入 defaultConfig，debug/release 统一为 com.example.lixing.debug，release 包可直接覆盖历史安装；
- **版本清单服务**：Cloudbase 云函数 update-check（普通事件函数，内联 JSON / update.json 直链 / 代理 GitHub API 三种清单来源，10 分钟缓存），发布脚本 docs/release.sh 一条命令出包；
- **开源整理**：目录 gitignore 规范化、MIT 协议、CHANGELOG 拆分。

## 2026-08-05 ～ 2026-09-05

### 2026-09-05 调试签名固定进项目 + 覆盖安装迁移说明

- **问题**：`C:\Users\<用户>\.android\debug.keystore` 在 09-05 凌晨被系统清理工具删除重建，导致新包与设备上已装版本的签名不一致，覆盖安装被系统拒绝；
- **根治**：调试签名改为**项目内密钥库** `app/debug.keystore`（公开口令的调试密钥，进仓库无泄密风险），`build.gradle.kts` 显式声明 `signingConfigs.debug`。今后用户主目录的密钥库再被清掉也不影响签名链，所有设备从 `LiXing-debug-20260905a.apk` 起统一到同一签名；
- **已装旧版的一次性迁移**：签名无法找回（私钥不存在的包无法互相覆盖），旧版需先在「设置 → 数据」导出 `.lixingbackup`（含数据库、照片、设置）→ 卸载 → 安装新包 → 导入备份；墨墨 Token、AI 密钥、WebDAV/百度凭据不进备份，需重新配置一次。


### 2026-09-04 WebDAV 多端同步收尾 + 公式渲染修复 + 仓库清理

- **WebDAV 多端同步全量落地**（设置页「多端同步」）：WebDAV 客户端（PUT/GET/PROPFIND/MKCOL/DELETE）、每设备云端变更文件、Lamport 时钟 + 墓碑的 SQLite 触发器、纯函数合并内核（字段级 last-writer-wins + 墓碑删除，双设备收敛有单测）、加密凭据存储、`SyncRepository` 编排（启动首同步 + 空闲 30 秒去抖自动同步 + 手动撞自动提示不排队）、仅 Wi-Fi 开关、同步统计 UI；14 个实体主键迁移为 UUID 并保持 v9→v10 非破坏迁移；照片列不参与同步；
- **同步恢复点**：手动同步（及首次自动同步）前自动创建 `before_sync` 恢复点，独立槽位保留 3 份；百度网盘「恢复成功并校验 SHA-256 后才允许删除云端旧副本」门禁；
- **公式渲染修复**（真机截图诊断）：多行推导出现「公式无法渲染」占位框的根因是超宽公式拆分把 `\begin{array}` 内部的 `\\` 当安全断点，切出孤儿环境头尾。修复：拆分点增加环境深度守卫（绝不在环境内部切）；超宽 `array`/`aligned` 按行解包为独立公式；跨行 `\left[…\right]` 与 `cases`/`pmatrix` 整块保留；
- **JLatexMath 单测解锁**：查明 JVM 单测中 `TeXFormula` 静态初始化失败的根因（Android 版库从 assets 读配置，需先 `JLatexMathAndroid.init(context)`，与 AWT 无关）；`Application.onCreate` 显式补 init；新增 6 例真实解析器回归测试（array 支持验证、清洗管线端到端、占位符路径），全量 **307 例测试 0 失败**；
- **确认按钮时有时无修复**：根因是长回答撞上输出上限把动作 JSON 截断、旧解析器只抢救正文丢弃全部动作。解析器升级为三级容错（整包 → 任意位置提取 → 截断抢救已完整条目），新增 4 例回归测试；
- **公式拆分第四种坏例修复**：`\,` 等符号命令里的逗号被当成逗号断点，切出以孤立 `\` 结尾的非法片段（占位框显示 `-y\,\mathrm dx+x\`，逗号留在下一段渲染成 `,dy`）。修复：符号命令（`\,` `\;` `\!` `\{` `\}` 等）与反斜杠作为整体跳过，顺带修正 `\{`/`\}` 转义花括号污染深度计数的问题，新增 2 例回归测试；
- **AI 生成保活**：回答生成期间启动 `dataSync` 前台服务 + 低优先级常驻通知（引用计数支持照片链路嵌套，结束自动停止），降低熄屏/切后台导致「没有任何输出」的概率；
- **路线图更新**：「跨设备会话同步」标记为已由 WebDAV 同步落地；
- **墨墨进度加入自动同步**：新增「跟随多端同步自动打卡」开关（默认关闭）。开启后每次同步会顺带拉取墨墨进度并打卡单词任务，采用「只增不减」策略（完成比例上升才推进，绝不撤销已有打卡与积分）、两次执行至少间隔 15 分钟、遵守仅 Wi-Fi、未配置 Token 静默跳过；打卡结果随任务表正常跨设备同步；
- **仓库清理**：删除 7 个历史 APK、`backups/`（1.9GB 历史快照）、`.tmp/`、`recovery/`、根 `build/`、云函数部署 zip 与构建日志，共释放约 4.9GB；安装包只保留最新版本。全量 **320 例测试 0 失败**；安装包 `LiXing-debug-20260904e.apk`。

### 2026-09-01 表格渲染 + 确认入口按钮 + 闪退兜底

- AI 回答支持 GFM 表格：引入 `markwon-ext-tables`，`| 命题 | 关键差异 | 结论 |` 这类内容渲染为真正的表格，不再是带竖线的纯文本；
- 计划修改 / 英语积累的确认入口改为挂在**产生该方案的那条回答气泡下面**的两个独立按钮（「📋 确认计划调整（N 项）」「📖 确认英语积累（N 条）」），不再回复一回来就自动跳到全屏确认页，可以先读回答再进入；没有待确认内容时按钮完全不渲染；入口唯一（删除了列表里的待确认卡片）；
- 修复「生成最终答案时小概率闪退」：JLatexMath 遇到模型输出的非法 LaTeX（缺括号、残留 `\tag`、aligned 前导非法字符等）会把 `ParseException` 包成 `RuntimeException` 在 UI 线程抛出导致闪退。现在给插件配置 `errorHandler`，单条公式失败画「公式无法渲染」占位；`MarkdownAnswer` 整体兜底，整段渲染失败回退纯文本并把异常与原文片段写入本机 `assistant-render.log`（≤200KB 自动截断）；
- aligned→array 转换加防御：嵌套环境 / `\hline` / 行数 > 12 / 列数 > 6 时不转换，宁可显示源码也不生成必然解析失败的 array；
- 单元测试回归通过；新安装包 `LiXing-debug-20260901.apk`。

### 2026-08-30 AI 助手自动写入英语积累 + 公式渲染兜底

- AI 学习助手新增英语积累动作协议：模型可通过 `english_actions` 返回 `ADD_ENGLISH_ENTRY` / `UPDATE_ENGLISH_ENTRY` / `DELETE_ENGLISH_ENTRY`，由对话输入、拍照识别或材料总结触发；
- 新增独立的「英语积累变更」确认页，逐条展示变更前/变更后与调整原因，支持逐项勾选、全部拒绝和接受所选，写入后以 Snackbar 汇总“新增 X 条，修改 Y 条，删除 Z 条”；
- 本地校验：类型（WORD/PHRASE/SENTENCE）、英文内容 ≤ 500 字符、释义 ≤ 2000 字符、修改/删除的 id 必须在库中真实存在；内容与现状相同的修改会被标记为不可应用；
- 英语积累上下文补上条目 id 并把上限提到 50 条，便于模型引用真实条目并避免重复新增；`EnglishEntryRepository` 新增 `get(id)`；
- 新增 `guardMissingEnglishActions`：用户明确要求写英语积累但模型没返回动作时，正文前会加上“本次没有写入任何内容”，不把模型的口头承诺当成执行结果；
- 系统提示词增补第 11 条协议，并明确英语条目删除是「不删除任何数据」的唯一例外；
- 公式渲染兜底：渲染前清洗 `\tag{}`、`\hspace{}`、`\vspace{}`、`[6pt]` 等 JLatexMath 不友好内容，并为落单的 `\begin{aligned}...\end{aligned}` 自动补 `$$` 并转换为 `array` 环境，修复小概率出现的公式显示为源码的问题；
- 移除 AI 助手顶栏的「英语积累」跳转按钮（英语积累的手动入口仍在今日页与专注页，AI 写入走新的动作协议）；
- 单元测试：`AssistantResponseParserTest` 新增英语动作解析与非法动作跳过的用例；`AssistantModelClientTest`、`AssistantResponseParserTest` 均通过。


### 2026-08-11 AI 拍题管线重构与公式渲染兼容

- AI 今日临时计划支持逐项“仅今日跳过”：用户说今天不做或删除今日任务时，助手生成非破坏性的跳过方案，不删除记录、不改模板和未来安排，也不计漏卡；
- AI 今日请假新增单条 `TAKE_TODAY_OFF` / `KEEP_TODAY_TASKS` 批量协议，避免任务较多时逐项动作导致结构化 JSON 截断；整日请假本地校验月度额度，缺少合法动作时明确提示没有修改数据；
- AI 学习助手新增独立的「计划修改方案」确认页，按仅今日/长期计划分区展示修改前后，支持逐项选择、全部拒绝和接受所选；今日临时调整可改目标量或移动时段，但不会改任务模板与未来任务，接受前仍自动创建 `before_ai_apply` 恢复点；
- 拍题路由重构：「多模态模型」开关开启时照片直发模型识题作答（EXIF 方向校正 + 最长边 2048 压缩）；关闭时走本地数学 OCR 预览，本地 OCR 明确定位为纯文本模型兜底；
- 本地数学 OCR 增强：Pix2Text MFD 对宽幅/窄幅图自动重叠分块；MFR 改为与官方预处理一致的 384×384 直接缩放；ML Kit 全文兜底，公式结果只替换通过质量门控的非中文区域，低置信/幻觉公式自动回退普通文字；表格经网格线检测输出 Markdown；
- 回答公式渲染兼容：`$...$`、`\(...\)`、`\[...\]` 归一化为 `$$...$$`，解决部分模型公式显示为源码的问题；代码块、行内代码与金额不误转换；
- 思考强度按模型配置可调（低/中/高，默认低），答案截断时自动续写一次；
- 长公式自动换行：JLatexMath 实测宽度，超宽公式在顶层安全断点拆行、超宽行内公式转块级，解决右侧溢出裁剪；
- AI 设置新增「拍题识题模型」与「饮食校准模型」两个多模态默认选择；纯文本模型拍题时先由识题模型转写进可编辑预览，本地 OCR 退为兜底；饮食拍照可由校准模型估算名称/克重/营养，失败回退端侧模型；
- 拍题交互简化为“发照片直达回答”：转写/本地 OCR 在后台静默完成，不再弹出识别进度与可编辑预览（多模态直送、识题模型转写、本地 OCR 三条路径均直接进回答流）；
- 拍题裁剪放宽：最小裁剪尺寸由 72dp 降为 24dp，单行长题可以裁出很扁的框；比例预设新增 3:1 横条；
- 真机回归方式：日常设备拒绝安装测试组件，改为仅 debug 构建的一次性诊断 Activity 读图写结果，入口用完已移除，正式包不含；
- 现状说明：百度网盘下载尚无前台服务与断点续传，熄屏/切网可能中断下载（`Software caused connection abort`），需保持前台重试；联网搜索已接入 DeepSeek（Responses API）与小米 MiMo（Chat Completions web_search 工具）两种服务端内置搜索。

### 2026-08-09 AI 学习助手（可选联网）

- 新增 AI 学习助手页：对话问答、可选上下文、快捷提问；默认关闭；
- 今日页任务卡在副标题行直接显示学科名（学科色），无需再靠色点猜学科；
- 助手可返回四类计划修改建议（调整时段/模板、新增模板、调整今日目标），需逐条勾选确认，应用前自动创建恢复点；
- 采用 App 直连 OpenAI 兼容接口：设置页配置接口地址、模型名与 API 密钥，可任意切换 DeepSeek / 月之暗面 / 智谱等服务商，无需部署或修改云服务；
- API 密钥用 Android Keystore 加密，不进版本备份；
- 新增动作协议解析与计划修改执行器单元测试；`cloudbase/ai-assistant/` 保留为可选的自建网关方案。
- 上下文改为「模型自动预判 + 手动勾选」：发送时先让模型按问题选择需要附带的本机数据，手动勾选的始终附带；
- 修复墨墨 get_today_items 请求携带显式 null 被服务端拒绝的问题（explicitNulls = false）；
- 今日页右上角精简为「饮食 / 回顾」，英语积累与 AI 助手入口移至专注页统计下方；
- AI 会话持久化：新增 `assistant_conversation` / `assistant_message` 表（v6→v7 非破坏迁移，纳入版本备份），助手页右上角「历史对话」可回看续聊、逐条删除；计划建议仍只在内存中逐条确认。
- 助手新增拍题：多模态开关决定「直接发图」或「本地 ML Kit OCR 提取文字后发送」，OCR 完全离线；识别不到文字会提示。
- 拍题支持相机连续拍摄和相册多选（最多 9 张），修复 OCR 正文未进入模型请求的问题；输入区跟随键盘上移，消息显示可点击放大的图片缩略图；AI 图片消息与图片文件纳入会话历史和版本化备份（数据库 v7→v8）。
- AI 设置支持保存、编辑、删除和切换多套 OpenAI 兼容模型配置；每套配置独立记录 API 密钥与多模态能力，旧的单模型设置会自动迁移为第一套配置。
- OCR 正文改为只发送给模型，聊天气泡只显示用户主动输入的文字和图片缩略图；数据库升级至 v9，通过非破坏性 v8→v9 迁移增加可空的界面展示字段，旧消息继续按原样显示。

### 2026-08-09 英语积累

- 今日页新增“英语”入口；无学习计划时同样可以进入；
- 新增本地英语积累本，可录入单词、短语、句子及释义，支持搜索、分类筛选、编辑和删除；
- 数据库升级至 v6，采用非破坏性 v5→v6 迁移，`english_entry` 已纳入本机/网盘版本化备份；
- 新增 DAO 搜索筛选、迁移保留旧数据及备份恢复单元测试。

### 计划与今日页

- 修复计划时段时间看似可编辑但无法正确写入的问题；可编辑时间统一经过严格解析并兼容全角字符；
- 修复 `21:35-22:15` 等有效时间被误判无效的问题；
- 新增时段默认时间改为 `00:00-00:00`；
- 计划修改后立即重新协调当天已物化任务，今日页无需退出重进即可更新；
- 即使时段暂时没有任务，今日页也保留对应时段卡片，新增“睡前”等时段后可见。

### 专注计时

- 修复番茄钟无法检测切出 App 的问题；专注进行中切到其他应用会记录待提交中断；
- “放弃本次”不写入中断次数；只有用户接受并记录本次计时后，时长与中断次数才一起提交。

### 版本化备份与多端共享

- 新增完整 `.lixingbackup` 格式：Room 全表、非敏感设置、照片、清单、数据库版本和 SHA-256 校验；
- 支持本机版本轮转、系统文件选择器导入导出、恢复前 `before_restore` 自动恢复点；
- 新增百度网盘 OAuth、Android Keystore AES-GCM 凭证加密，以及分片上传、版本列表、下载校验和确认恢复；
- OAuth 中转由受阻的 Cloudflare Workers 迁移至腾讯云 CloudBase，已在真机验证授权、上传、列表、下载与恢复确认全链路。

### 自我承诺到期结算

- 补齐承诺功能：此前 `commitment` 表、枚举、DAO、幂等键已在 v2 schema 中就位，后续已通过 v3→v4 迁移增加多维度指标和自定义奖励；
- 新增 `domain/settle/CommitmentSettler`（纯函数）：结算判定 + 进行中实时进度，百分比向下取整保证显示与结论自洽；
- `GamificationRepository` 新增承诺读写、进度查询与 `settleExpiredCommitments()`；
- 结算挂在 `SettleDayUseCase.onAppStart()` 与每日 Worker，均排在当天结算之后（顺序由 `onAppStart()` 内部保证）；
- 「我的」页新增立约对话框、进行中卡片（含实时进度条）与历史回看；承诺进度跟随今日 `day_record` 变化刷新，打卡后立即可见；
- 到期结果通过今日页弹窗逐条回看，连带当初写给自己的那句话；
- 新增承诺相关单测，调试 APK 位于 `app/build/outputs/apk/debug/app-debug.apk`。

### 2026-08-06 反馈修订

- 锁屏/熄屏不再误计为番茄钟切出中断；
- 每个时段可设置最低完成数，最低数全部完成为 100%，超额完成允许超过 100%；
- 首页完成度环、周报/月报及时段统计采用新的时段完成口径；
- 墨墨累计背诵时长按增量计入英语投入，避免重复同步累加；
- 阶段性立誓新增累计专注分钟、达标天数和自定义奖励；数据库升级至 v4；
- 年度热力图改为实时观察 `day_record`，与自定义学习日起点使用同一学习日，并按最近 12 个自然月逐月翻页。

### 文档修正

- 路线图第 3 项「今日页已完成任务的撤销入口」实际早已实现（`CheckInUseCase.revoke()`、`CheckInDialog` 撤销按钮、积分按流水回滚），本轮从待办中移除。

